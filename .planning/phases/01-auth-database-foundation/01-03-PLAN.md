---
phase: 01-auth-database-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/app/api/admin/login/route.ts
  - src/app/api/admin/login/route.test.ts
  - src/proxy.ts
  - src/app/admin/login/page.tsx
  - src/components/forms/AdminLoginForm.tsx
  - src/app/admin/dashboard/page.tsx
  - src/app/admin/layout.tsx
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04]

must_haves:
  truths:
    - "Owner can POST correct email/password to /api/admin/login and receive a 200 with an httpOnly admin_session cookie"
    - "Wrong credentials return 401 and record a failed login_attempts row"
    - "After 5 failed attempts within 15 minutes, further attempts return 429/423 even with correct credentials"
    - "Visiting /admin/dashboard without a valid session cookie redirects to /admin/login"
    - "The login page reuses the dark theme, bg-image.jpg background, and .btn-metallic button (D-01)"
  artifacts:
    - path: "src/app/api/admin/login/route.ts"
      provides: "POST login: CSRF -> lockout check -> validate -> bcrypt compare -> set JWT cookie -> record attempt"
      exports: ["POST"]
    - path: "src/proxy.ts"
      provides: "Real /admin/* session-redirect (replaces Plan 01 smoke version)"
      contains: "verifySession"
    - path: "src/app/admin/login/page.tsx"
      provides: "Login page matching public dark theme (D-01)"
      contains: "bg-image.jpg"
    - path: "src/app/admin/dashboard/page.tsx"
      provides: "Authenticated page proving session round-trips; calls requireAdmin()"
      contains: "requireAdmin"
  key_links:
    - from: "src/app/api/admin/login/route.ts"
      to: "src/lib/auth.ts"
      via: "isLockedOut + recordLoginAttempt + signSession"
      pattern: "isLockedOut|recordLoginAttempt|signSession"
    - from: "src/proxy.ts"
      to: "src/lib/auth.ts verifySession"
      via: "cookie check"
      pattern: "verifySession"
    - from: "src/app/admin/dashboard/page.tsx"
      to: "src/lib/auth.ts requireAdmin"
      via: "page-level guard"
      pattern: "requireAdmin"
---

<objective>
Wire the auth primitives from Plan 02 into the actual user-facing login: the `/api/admin/login` route (defense-in-depth ordering matching the existing register route), the REAL `proxy.ts` (replacing Plan 01's smoke version), the dark-themed login page (D-01), and a minimal authenticated dashboard that proves the session round-trips. This plan closes all four AUTH requirements.

Purpose: This is the wiring wave. Plan 02 defined and tested the contracts in isolation; this plan connects them end-to-end so the owner can actually log in (AUTH-01), stay logged in across refresh (AUTH-02), be redirected when unauthenticated (AUTH-03), and be locked out after repeated failures (AUTH-04). Defense-in-depth: proxy.ts redirects at the page layer AND each admin page calls requireAdmin() — so AUTH-03 holds even if proxy.ts surprised us on the Plan 01 smoke test.
Output: A working login flow, a protected /admin area, and an integration test for the login route.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-auth-database-foundation/01-CONTEXT.md
@.planning/phases/01-auth-database-foundation/01-RESEARCH.md
@.planning/phases/01-auth-database-foundation/PROVISIONING-NOTES.md
@CLAUDE.md
@SECURITY-AUDIT.md
@src/app/api/register/route.ts
@src/lib/csrf.ts
@src/lib/client-ip.ts
@src/app/globals.css

<interfaces>
<!-- Contracts from Plan 02 this plan imports. Use directly — no exploration needed. -->

From src/lib/auth.ts:
```typescript
export interface SessionPayload { sub: string; email: string; }
export const COOKIE_NAME: string;  // "admin_session"
export function signSession(payload: SessionPayload): Promise<string>;
export function verifySession(token: string): Promise<SessionPayload | null>;
export function requireAdmin(): Promise<SessionPayload | null>;
export function recordLoginAttempt(args: { email: string; ipAddress: string; succeeded: boolean }): Promise<void>;
export function isLockedOut(email: string): Promise<boolean>;
```
From src/lib/db/schema.ts: `adminUsers`, `loginAttempts`
From src/lib/db/index.ts: `db`
From src/lib/csrf.ts: `isTrustedOrigin(req: Request): boolean`
From src/lib/client-ip.ts: `getClientIp(req: Request): string`

Existing register route defense-in-depth ORDER to mirror (src/app/api/register/route.ts):
CSRF origin check -> rate limit -> parse body -> validate -> business logic.
For login, SUBSTITUTE the in-memory rate limit with isLockedOut() (DB-backed) — RESEARCH Pitfall 1.

Design tokens (src/app/globals.css): `.btn-metallic` (primary CTA), `.btn-glass`, `--bg-primary`, `--text-secondary`, `--border-color`. Background pattern (every page): fixed inset-0 -z-10 next/image fill of /bg-image.jpg.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: /api/admin/login route with DB-backed lockout</name>
  <behavior>
    - POST with correct {email, password} -> 200, response sets an httpOnly admin_session cookie, records a succeeded=true login_attempts row.
    - POST with wrong password -> 401, records a succeeded=false row, NO cookie set.
    - POST with unknown email -> 401 (same shape/timing as wrong password — do not reveal whether the email exists), records a succeeded=false row.
    - POST when isLockedOut(email) is true (>=5 recent failures) -> 429 (or 423 Locked), WITHOUT performing the bcrypt compare, even if the password would have been correct.
    - POST with a cross-site Origin header (isTrustedOrigin false) -> 403.
    - POST with malformed body (missing email/password) -> 400/422.
  </behavior>
  <read_first>
    - src/app/api/register/route.ts (the canonical defense-in-depth ordering + JSON error response shapes to mirror)
    - src/lib/csrf.ts (isTrustedOrigin)
    - src/lib/client-ip.ts (getClientIp)
    - .planning/phases/01-auth-database-foundation/01-RESEARCH.md (Pattern 2 cookie-set on login, Pattern 3 bcrypt.compare, Pattern 5 lockout)
    - SECURITY-AUDIT.md (its "no CSRF token / in-memory limiter" accepted risks become INVALID once this cookie-auth route ships — informs the follow-up review flagged in Plan 04)
  </read_first>
  <action>
    Write `src/app/api/admin/login/route.test.ts` FIRST covering the behaviors above (RED), then implement `src/app/api/admin/login/route.ts` (GREEN).

    Route implementation — exact ordering (mirrors register route, substituting DB lockout for in-memory limiter):
    1. CSRF: `if (!isTrustedOrigin(req)) return NextResponse.json({ error: "Forbidden" }, { status: 403 });`
    2. Parse body in try/catch -> 400 on failure.
    3. Validate with zod: `z.object({ email: z.string().email(), password: z.string().min(1) })`; on failure return 422 (mirror register's `{ error, fields }` shape). Lowercase the email (`.toLowerCase()`) to match the stored/seeded email.
    4. Lockout check BEFORE any bcrypt work: `if (await isLockedOut(email)) return NextResponse.json({ error: "Too many attempts. Try again later." }, { status: 429, headers: { "Retry-After": "900" } });`
    5. Look up admin: `const [user] = await db.select().from(adminUsers).where(eq(adminUsers.email, email));`
    6. Compare: if no user OR `!(await bcrypt.compare(password, user.passwordHash))` -> `await recordLoginAttempt({ email, ipAddress: getClientIp(req), succeeded: false }); return NextResponse.json({ error: "Invalid credentials." }, { status: 401 });` (identical response for unknown-email and wrong-password — no user enumeration).
    7. Success: `await recordLoginAttempt({ email, ipAddress: getClientIp(req), succeeded: true });` then `const token = await signSession({ sub: String(user.id), email: user.email });` then set cookie via `const cookieStore = await cookies(); cookieStore.set(COOKIE_NAME, token, { httpOnly: true, secure: process.env.NODE_ENV === "production", sameSite: "strict", path: "/", maxAge: 60*60*8 });` then `return NextResponse.json({ ok: true });`
    8. `import bcrypt from "bcryptjs";` — use `bcrypt.compare`.

    Test approach: import the POST handler and call it with constructed `Request` objects (set Origin/host headers so isTrustedOrigin passes). Seed a known admin row in test setup (reuse the seed logic or insert directly) so the correct-password case can pass. Use a unique throwaway email per run. For the lockout case, insert 5 failed login_attempts rows for the email first, then assert the correct-password POST is still rejected with 429.
  </action>
  <verify>
    <automated>npm test -- src/app/api/admin/login/route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/app/api/admin/login/route.ts exports `POST`
    - route.ts calls `isTrustedOrigin`, `isLockedOut`, `recordLoginAttempt`, `signSession`, and `bcrypt.compare` (grep each)
    - route.ts does NOT import `checkRateLimit` or from `@/lib/rate-limiter` (DB-backed lockout only — Pitfall 1)
    - route.ts sets the cookie with `httpOnly: true` and `sameSite: "strict"`
    - unknown-email and wrong-password both return status 401 with the same error body (no enumeration)
    - `npm test -- src/app/api/admin/login/route.test.ts` exits 0
  </acceptance_criteria>
  <done>Login route implemented with full defense-in-depth, DB-backed lockout precedes bcrypt, no user enumeration, cookie set on success; integration tests green (AUTH-01 + AUTH-04).</done>
</task>

<task type="auto">
  <name>Task 2: Real proxy.ts + admin layout + dashboard (defense-in-depth)</name>
  <read_first>
    - src/proxy.ts (the Plan 01 smoke version being replaced)
    - .planning/phases/01-auth-database-foundation/PROVISIONING-NOTES.md (OQ1 result — did the smoke proxy fire on real Netlify? Determines how much weight proxy.ts carries vs page-level requireAdmin())
    - .planning/phases/01-auth-database-foundation/01-RESEARCH.md (Pattern 1 proxy.ts + Pitfall 2 placement/double-guard)
    - src/lib/auth.ts (verifySession, requireAdmin, COOKIE_NAME)
    - src/app/layout.tsx (root layout — confirm the bg-image.jpg fixed-background pattern to replicate in admin layout)
  </read_first>
  <action>
    Replace the smoke `src/proxy.ts` with real session-redirect logic, and add page-level guards so AUTH-03 holds regardless of proxy behavior (defense-in-depth, RESEARCH Pitfall 2).

    1. Replace `src/proxy.ts` body with:
       ```typescript
       import { NextResponse } from "next/server";
       import type { NextRequest } from "next/server";
       import { verifySession } from "@/lib/auth";
       import { COOKIE_NAME } from "@/lib/auth";

       export async function proxy(request: NextRequest) {
         const { pathname } = request.nextUrl;
         if (!pathname.startsWith("/admin") || pathname === "/admin/login") {
           return NextResponse.next();
         }
         const token = request.cookies.get(COOKIE_NAME)?.value;
         const session = token ? await verifySession(token) : null;
         if (!session) {
           return NextResponse.redirect(new URL("/admin/login", request.url));
         }
         return NextResponse.next();
       }

       export const config = { matcher: ["/admin/:path*"] };
       ```
       Keep it at `src/proxy.ts` (NOT repo root — Pitfall 2).

    2. Create `src/app/admin/layout.tsx` (Server Component) that wraps admin pages in the SAME dark-theme background pattern as the public site (D-01): a `<div className="relative">` with a `<div className="fixed inset-0 -z-10">` holding a `next/image` fill of `/bg-image.jpg` (`object-cover object-center`, `priority`). Reuse the exact pattern from src/app/layout.tsx / a public page — do NOT add a separate admin background or `bg-slate-900`. Use Tailwind v4 `bg-(--bg-primary)` syntax, NOT `bg-[var(--bg-primary)]`.

    3. Create `src/app/admin/dashboard/page.tsx` (Server Component): call `const session = await requireAdmin();` at the top; `if (!session) redirect("/admin/login");` (page-level guard — the second defense layer). Render a minimal authenticated view: a heading "Admin Dashboard" and the logged-in email. This page exists to PROVE the session round-trips (AUTH-02) — no real admin features yet (those are Phase 2+).

    4. Do NOT add any link to /admin anywhere on the public site (D-04) — this plan touches zero public-site files (no nav/footer edits). Confirm by grepping that no public component references "/admin".
  </action>
  <verify>
    <automated>npx tsc --noEmit && node -e "const s=require('fs').readFileSync('src/proxy.ts','utf8'); if(!s.includes('verifySession')||!s.includes('matcher')) process.exit(1); const d=require('fs').readFileSync('src/app/admin/dashboard/page.tsx','utf8'); if(!d.includes('requireAdmin')) process.exit(1); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - src/proxy.ts imports `verifySession` and `COOKIE_NAME` from @/lib/auth and redirects to /admin/login when no session
    - src/proxy.ts is at src/proxy.ts (NOT ./proxy.ts) and contains `matcher: ["/admin/:path*"]`
    - src/app/admin/layout.tsx contains `bg-image.jpg` and `fixed inset-0 -z-10` (reuses the one site background, D-01)
    - src/app/admin/layout.tsx uses no `bg-[var(` bracket syntax (Tailwind v4 `bg-(--...)` only)
    - src/app/admin/dashboard/page.tsx calls `requireAdmin()` and redirects when null (page-level guard)
    - No public-site component references the string "/admin" (D-04) — grep src/components and public pages returns nothing
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Real proxy.ts redirects unauthenticated /admin/* (AUTH-03), admin dashboard double-guards with requireAdmin() and proves session persistence (AUTH-02), admin area uses the one dark-theme background (D-01), zero public trace of /admin (D-04).</done>
</task>

<task type="auto">
  <name>Task 3: Dark-themed login page + login form (D-01)</name>
  <read_first>
    - .planning/phases/01-auth-database-foundation/01-CONTEXT.md (D-01 login look-and-feel)
    - src/app/globals.css (.btn-metallic, .btn-glass, --bg-primary, --text-secondary, --border-color tokens)
    - src/components/forms/ContactForm.tsx (existing client-form pattern: "use client", fetch to API, error/loading state, toast/inline error handling to mirror)
    - src/lib/csrf.ts (the login POST must come from a same-origin fetch so isTrustedOrigin passes)
  </read_first>
  <action>
    Build the login page (D-01: matches public dark theme, reuses `.btn-metallic`, no separate visual language).

    1. Create `src/app/admin/login/page.tsx` (Server Component, no auth guard — this is the ONE unauthenticated page under /admin). It renders the shared background (inherited from the admin layout in Task 2) and the `<AdminLoginForm />` client component, centered in a card using existing design tokens (`bg-(--bg-primary)`, `text-(--text-secondary)`, `border-(--border-color)`). Use Tailwind v4 `bg-(--...)` syntax only.

    2. Create `src/components/forms/AdminLoginForm.tsx` (`"use client"`):
       - Controlled email + password inputs.
       - On submit: `fetch("/api/admin/login", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ email, password }) })` (same-origin so isTrustedOrigin passes; the browser sends the Origin header automatically).
       - On 200: `window.location.href = "/admin/dashboard"` (full navigation so the new cookie is sent and proxy.ts/requireAdmin let it through).
       - On 401: show inline "Invalid email or password."
       - On 429/423: show "Too many attempts. Please try again in a few minutes."
       - On other errors: generic "Something went wrong. Please try again."
       - Loading state disables the submit button.
       - Submit button uses `className="btn-metallic"` (the primary CTA class — do NOT invent a new button style).
       - Include a "Forgot password?" link to `/admin/forgot-password` (the page is built in Plan 04 — the link target is fine to reference now).
       - Mirror the error/loading/accessibility conventions from ContactForm.tsx (aria-labels, real `<label>` elements).
  </action>
  <verify>
    <automated>npx tsc --noEmit && node -e "const f=require('fs').readFileSync('src/components/forms/AdminLoginForm.tsx','utf8'); if(!f.includes('use client')||!f.includes('/api/admin/login')||!f.includes('btn-metallic')) process.exit(1); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - src/app/admin/login/page.tsx exists and renders AdminLoginForm
    - src/components/forms/AdminLoginForm.tsx starts with "use client" and POSTs to /api/admin/login
    - AdminLoginForm submit button uses class `btn-metallic` (D-01 — reuses existing button class)
    - login page uses no `bg-[var(` bracket syntax (Tailwind v4 `bg-(--...)` only)
    - AdminLoginForm handles 200/401/429 responses distinctly and links to /admin/forgot-password
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Login page matches the public dark theme (D-01), form submits same-origin to the login route, handles success/invalid/lockout states, reuses .btn-metallic; AUTH-01 user-facing entry complete.</done>
</task>

</tasks>

<verification>
- `npm test` passes (login route integration tests green)
- `npx tsc --noEmit` clean
- proxy.ts redirects unauthenticated /admin/* ; dashboard double-guards with requireAdmin()
- login page reuses dark theme + .btn-metallic ; no public-site trace of /admin (D-04)
- Manual (per VALIDATION.md): on a real Netlify deploy, log in with seeded creds, reload dashboard (session persists = AUTH-02), then visit /admin/dashboard in a private window (redirects = AUTH-03)
</verification>

<success_criteria>
- AUTH-01: owner logs in with seeded email/password, gets a session cookie
- AUTH-02: session persists across refresh (httpOnly/secure/strict cookie + requireAdmin re-check)
- AUTH-03: unauthenticated /admin/* redirects to /admin/login (proxy.ts + page-level guard)
- AUTH-04: 5 failed attempts within 15 min lock out further attempts, read from DB rows
- D-01 (dark theme login) and D-04 (no public /admin trace) honored
</success_criteria>

<output>
After completion, create `.planning/phases/01-auth-database-foundation/01-03-SUMMARY.md`
</output>
