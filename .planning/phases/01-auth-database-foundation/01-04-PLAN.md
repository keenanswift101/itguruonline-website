---
phase: 01-auth-database-foundation
plan: 04
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - src/lib/auth.ts
  - src/lib/email.ts
  - src/app/api/admin/forgot-password/route.ts
  - src/app/api/admin/reset-password/route.ts
  - src/app/admin/forgot-password/page.tsx
  - src/app/admin/reset-password/page.tsx
  - src/components/forms/ForgotPasswordForm.tsx
  - src/components/forms/ResetPasswordForm.tsx
  - src/app/api/admin/reset-password/route.test.ts
  - SECURITY-AUDIT.md
autonomous: false
requirements: [AUTH-01, AUTH-02]

must_haves:
  truths:
    - "Owner can request a password reset and a one-time link is emailed to ambrose@it-guru.co.za (D-03)"
    - "The reset link sets a hashed, time-limited reset token on the admin_users row"
    - "Following a valid, unexpired reset link lets the owner set a new password; the new password then works at login"
    - "An expired or already-used reset token is rejected"
    - "SECURITY-AUDIT.md is updated to flag that cookie-auth now invalidates the prior accepted CSRF/in-memory-limiter risk notes"
  artifacts:
    - path: "src/app/api/admin/forgot-password/route.ts"
      provides: "POST: generate reset token, store hash+expiry, email link (D-02/D-03)"
      exports: ["POST"]
    - path: "src/app/api/admin/reset-password/route.ts"
      provides: "POST: verify token, set new bcrypt password hash, clear token"
      exports: ["POST"]
    - path: "src/app/admin/forgot-password/page.tsx"
      provides: "Request-reset page (dark theme, D-01)"
      contains: "btn-metallic"
    - path: "src/app/admin/reset-password/page.tsx"
      provides: "Set-new-password page consuming the token query param"
      contains: "ResetPasswordForm"
  key_links:
    - from: "src/app/api/admin/forgot-password/route.ts"
      to: "src/lib/email.ts emailLayout"
      via: "Resend send of the reset link"
      pattern: "emailLayout"
    - from: "src/app/api/admin/reset-password/route.ts"
      to: "admin_users.resetToken / resetTokenExpiresAt"
      via: "token verify + password update"
      pattern: "resetToken"
---

<objective>
Build the "Forgot password" recovery flow (D-02/D-03): a request-reset page that emails a one-time, time-limited reset link to `ambrose@it-guru.co.za` via the existing Resend integration, and a set-new-password page that consumes the token. Also discharge the SECURITY-AUDIT.md follow-up that this phase's introduction of cookie-based auth makes necessary, and run the final browser-level verification of the whole auth flow.

Purpose: D-02 explicitly requires self-service recovery so the owner is never locked out needing developer DB intervention. This supports AUTH-01 (the owner can always regain login access) and AUTH-02 (recovery sets a new password that establishes a fresh valid session). The reset email MUST follow the existing table-based inline-style email pattern (Outlook compatibility). This plan also closes STATE.md's logged blocker that a fresh security review is owed once cookie auth ships.
Output: A complete, tested password-reset flow, an updated SECURITY-AUDIT.md, and a verified end-to-end auth experience.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-auth-database-foundation/01-CONTEXT.md
@.planning/phases/01-auth-database-foundation/01-RESEARCH.md
@CLAUDE.md
@SECURITY-AUDIT.md
@src/lib/email.ts
@src/lib/csrf.ts
@src/lib/client-ip.ts
@src/lib/auth.ts

<interfaces>
<!-- Contracts from Plans 02/03 this plan builds on. -->
From src/lib/auth.ts: signSession, verifySession, COOKIE_NAME, isLockedOut, recordLoginAttempt (Plan 02)
From src/lib/db/schema.ts: adminUsers (has resetToken, resetTokenExpiresAt columns already), loginAttempts
From src/lib/db/index.ts: db
From src/lib/email.ts:
```typescript
export function emailLayout(title: string, bodyHtml: string): string;  // table-based, inline styles ONLY
export function escapeHtml(value: string): string;
export const ADMIN_EMAIL: string;
// sendEmail(...) — existing send helper (confirm exact signature in email.ts before use)
```
From src/lib/csrf.ts: isTrustedOrigin
From src/lib/client-ip.ts: getClientIp

CLAUDE.md hard rule for the reset email: emailLayout() is table-based with EVERY style inline — NO <style> blocks, NO flexbox/grid (Outlook/webmail strip them). The reset email body must follow this exact pattern.
D-03: the reset link is always sent to ambrose@it-guru.co.za (the confirmed working mailbox), regardless of the email typed into the forgot-password form.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reset-token helpers + forgot/reset API routes</name>
  <behavior>
    - createResetToken(email): generates a random token, stores its HASH + an expiry (1 hour from now) on the matching admin_users row, returns the RAW token (for the email link). Returns null/no-op if no matching admin (but the route still responds identically — no enumeration).
    - consumeResetToken(rawToken, newPassword): if a non-expired admin_users row matches the token hash, sets a new bcrypt password hash, clears resetToken + resetTokenExpiresAt, returns true. If token unknown/expired/already-cleared, returns false.
    - An expired token (set resetTokenExpiresAt in the past) is rejected by consumeResetToken.
    - After a successful reset, the OLD reset token no longer works (it was cleared).
  </behavior>
  <read_first>
    - src/lib/auth.ts (existing exports; add reset helpers here alongside them)
    - src/lib/db/schema.ts (adminUsers.resetToken + resetTokenExpiresAt columns — already exist from Plan 02)
    - src/lib/email.ts (emailLayout, escapeHtml, sendEmail signature, ADMIN_EMAIL — confirm exact send function name/params before calling)
    - .planning/phases/01-auth-database-foundation/01-CONTEXT.md (D-02 Resend reset link, D-03 send to ambrose@it-guru.co.za, discretion: ~1h expiry)
    - src/app/api/register/route.ts (defense-in-depth ordering to mirror in both new routes)
  </read_first>
  <action>
    Add reset helpers to `src/lib/auth.ts`, then build two API routes. Write `src/app/api/admin/reset-password/route.test.ts` FIRST covering the consume behaviors (RED), then implement (GREEN).

    Helpers in src/lib/auth.ts (concrete):
    - `const RESET_TOKEN_TTL_MINUTES = 60;` (Claude's-discretion ~1h per CONTEXT.md, documented inline)
    - `createResetToken(email)`: `const raw = crypto.randomBytes(32).toString("hex");` `const tokenHash = await bcrypt.hash(raw, 12);` set `resetToken: tokenHash, resetTokenExpiresAt: new Date(Date.now() + RESET_TOKEN_TTL_MINUTES*60_000)` on the admin row matching email; return `raw` (or null if no admin). Store the HASH, never the raw token.
    - `consumeResetToken(rawToken, newPassword)`: load admin rows where resetToken is not null AND resetTokenExpiresAt > now; for the candidate, `await bcrypt.compare(rawToken, row.resetToken)`; on match set `passwordHash = await bcrypt.hash(newPassword, 12)`, `resetToken = null`, `resetTokenExpiresAt = null`; return true. Else false.
    - `import crypto from "crypto";` `import bcrypt from "bcryptjs";`

    Route `src/app/api/admin/forgot-password/route.ts` (POST):
    1. isTrustedOrigin check -> 403.
    2. In-memory rate limit IS acceptable here (this is NOT the login lockout path — it's a send-email endpoint; reuse checkRateLimit from src/lib/rate-limiter.ts keyed `forgot:${getClientIp(req)}` to prevent email-bombing) -> 429.
    3. Parse + zod-validate { email }.
    4. `const raw = await createResetToken(email.toLowerCase());`
    5. If a token was created, build the reset URL `${process.env.NEXT_PUBLIC_BASE_URL ?? "https://it-guru.co.za"}/admin/reset-password?token=${raw}` and send via emailLayout()/sendEmail to `ambrose@it-guru.co.za` (D-03 — hardcode this recipient, do NOT send to the form-entered email). Email body table-based + inline styles only (escapeHtml any dynamic text).
    6. ALWAYS return the same 200 `{ ok: true }` whether or not the email matched an admin (no account enumeration).

    Route `src/app/api/admin/reset-password/route.ts` (POST):
    1. isTrustedOrigin -> 403.
    2. Parse + zod-validate { token: string, password: string.min(8) }.
    3. `const ok = await consumeResetToken(token, password);`
    4. `ok ? NextResponse.json({ ok: true }) : NextResponse.json({ error: "Invalid or expired reset link." }, { status: 400 });`

    Test: seed an admin, call createResetToken, assert a hashed token + future expiry are stored; call consumeResetToken with the raw token + new password, assert true and that the new password now bcrypt-compares against the stored hash; assert a second consume with the same raw token returns false (cleared); assert an expired token (manually set past expiry) returns false.
  </action>
  <verify>
    <automated>npm test -- src/app/api/admin/reset-password/route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/auth.ts exports `createResetToken` and `consumeResetToken`
    - src/lib/auth.ts stores a bcrypt HASH of the reset token (grep: `bcrypt.hash` near reset token), never the raw token in the DB
    - forgot-password route sends the email to the literal `ambrose@it-guru.co.za` (D-03), not the form email
    - forgot-password route returns the same 200 regardless of whether the email matched an admin (no enumeration)
    - reset-password route rejects expired/used tokens with 400
    - `npm test -- src/app/api/admin/reset-password/route.test.ts` exits 0
  </acceptance_criteria>
  <done>Reset-token helpers + both API routes implemented and tested; tokens stored hashed with 1h expiry, single-use, link always emailed to ambrose@it-guru.co.za (D-02/D-03), no enumeration.</done>
</task>

<task type="auto">
  <name>Task 2: Forgot/reset pages + forms (D-01) and reset email template</name>
  <read_first>
    - src/components/forms/AdminLoginForm.tsx (from Plan 03 — the client-form pattern + .btn-metallic + error/loading handling to mirror)
    - src/lib/email.ts (emailLayout — copy its table+inline-style structure for the reset email body)
    - src/app/admin/login/page.tsx (from Plan 03 — the dark-theme page shell to mirror, D-01)
    - CLAUDE.md (email rule: table-based, inline styles only)
  </read_first>
  <action>
    Build the two recovery pages + forms (D-01 dark theme, reuse .btn-metallic), and the reset email body.

    1. `src/app/admin/forgot-password/page.tsx` (Server Component, unauthenticated): dark-theme shell mirroring login page, renders `<ForgotPasswordForm />`.

    2. `src/components/forms/ForgotPasswordForm.tsx` ("use client"): email input; POSTs to /api/admin/forgot-password same-origin; on any 200 shows a NEUTRAL confirmation ("If that account exists, a reset link has been sent.") — must NOT reveal whether the email matched (mirrors the route's no-enumeration behavior); submit uses `btn-metallic`.

    3. `src/app/admin/reset-password/page.tsx` (Server Component, unauthenticated): reads the `token` search param, passes it to `<ResetPasswordForm token={token} />`.

    4. `src/components/forms/ResetPasswordForm.tsx` ("use client"): new-password + confirm-password inputs (client-side: must match, min 8 chars); POSTs { token, password } to /api/admin/reset-password; on 200 redirect to /admin/login with a success notice; on 400 show "This reset link is invalid or has expired."; submit uses `btn-metallic`.

    5. Reset email body (in the forgot-password route or a helper in email.ts): build with `emailLayout("Reset your IT-Guru admin password", body)` where `body` is table-based with EVERY style inline (NO <style> block, NO flexbox/grid — CLAUDE.md hard rule). Include the reset URL as a plain `<a>` styled inline (e.g. an inline-styled button-like anchor) and a fallback plain-text URL. escapeHtml any interpolated values.

    All pages use Tailwind v4 `bg-(--...)` syntax (no `bg-[var(` brackets), reuse the one bg-image.jpg background via the admin layout from Plan 03.
  </action>
  <verify>
    <automated>npx tsc --noEmit && node -e "const f=require('fs').readFileSync('src/components/forms/ResetPasswordForm.tsx','utf8'); if(!f.includes('use client')||!f.includes('/api/admin/reset-password')||!f.includes('btn-metallic')) process.exit(1); const g=require('fs').readFileSync('src/components/forms/ForgotPasswordForm.tsx','utf8'); if(!g.includes('/api/admin/forgot-password')) process.exit(1); console.log('ok')"