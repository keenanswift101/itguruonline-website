---
phase: 01-auth-database-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vitest.config.ts
  - drizzle.config.ts
  - src/proxy.ts
  - netlify/database/migrations/.gitkeep
  - .env.example
  - .planning/phases/01-auth-database-foundation/PROVISIONING-NOTES.md
autonomous: false
requirements: [AUTH-03]
user_setup:
  - service: netlify-database
    why: "Phase 1 core deliverable — Neon Postgres backing all admin data"
    env_vars:
      - name: NETLIFY_DATABASE_URL
        source: "Auto-injected by `netlify database init` across all deploy contexts — do NOT set manually"
    dashboard_config:
      - task: "Run `netlify database init` interactively (NOT --yes) and choose the Drizzle ORM query style when prompted"
        location: "Local terminal with Netlify CLI >= 26.0.0 authenticated to the it-guru-online site"
      - task: "Verify Netlify Database current pricing/free-tier terms (free-storage window flagged as closing 2026-07-01)"
        location: "https://docs.netlify.com/build/data-and-storage/netlify-database/billing-and-usage/"

must_haves:
  truths:
    - "A no-op proxy.ts redirect fires on a real Netlify build (not just next dev)"
    - "Netlify Database is provisioned and NETLIFY_DATABASE_URL is injected"
    - "vitest is installed and `npm test` runs an example test that passes"
  artifacts:
    - path: "src/proxy.ts"
      provides: "Smoke-test redirect proving proxy.ts works under @netlify/plugin-nextjs"
      contains: "export const config"
    - path: "vitest.config.ts"
      provides: "Test framework config for Route Handler + DB-state tests"
      contains: "defineConfig"
    - path: "drizzle.config.ts"
      provides: "drizzle-kit config pointed at Netlify's auto-apply migration dir"
      contains: "netlify/database/migrations"
    - path: ".planning/phases/01-auth-database-foundation/PROVISIONING-NOTES.md"
      provides: "Recorded answers to RESEARCH Open Questions 1-4 (proxy verified, @netlify/neon version, migration system choice, POPIA region)"
      contains: "proxy.ts"
  key_links:
    - from: "drizzle.config.ts"
      to: "netlify/database/migrations"
      via: "out directory"
      pattern: "netlify/database/migrations"
    - from: "package.json"
      to: "vitest"
      via: "test script"
      pattern: "\"test\":"
---

<objective>
De-risk the two highest-uncertainty unknowns in this phase BEFORE any real auth logic is written, and stand up the foundational tooling everything else depends on:

1. Prove `proxy.ts` actually fires under the real `@netlify/plugin-nextjs` adapter (RESEARCH Open Question 1 — the single highest-risk inference in this phase, currently MEDIUM confidence).
2. Provision Netlify Database (Neon Postgres) and resolve the exact `@netlify/neon` wiring + migration-system choice (RESEARCH Open Questions 2-4).
3. Install all phase dependencies and adopt `vitest` as the test framework (VALIDATION.md Wave 0 decision — made concrete here: YES, adopt vitest).

Purpose: This is Wave 0. Every later plan in this phase imports the DB client, calls auth helpers tested with vitest, and relies on `proxy.ts` redirecting. If proxy.ts silently does not fire on Netlify, or the DB driver is mis-wired, those failures must surface now — on a no-op smoke test — not after real login logic is layered on top.
Output: A verified-working `proxy.ts` smoke test deployed to a real Netlify build, a provisioned database, installed dependencies, a runnable `npm test`, and a PROVISIONING-NOTES.md recording the answers to the four open questions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-auth-database-foundation/01-CONTEXT.md
@.planning/phases/01-auth-database-foundation/01-RESEARCH.md
@.planning/phases/01-auth-database-foundation/01-VALIDATION.md
@CLAUDE.md
@SECURITY-AUDIT.md
@package.json
@netlify.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies, adopt vitest, verify package versions</name>
  <read_first>
    - package.json (current deps: only next, react, react-dom, resend — zero DB/auth/test deps)
    - .planning/phases/01-auth-database-foundation/01-RESEARCH.md (Standard Stack table + Installation block — exact versions verified 2026-06-30)
  </read_first>
  <action>
    Install the exact dependency set from RESEARCH.md Standard Stack. Run version verification FIRST, then install:

    1. Run version checks and capture output (these confirm the RESEARCH numbers haven't drifted):
       `npm view drizzle-orm version` (expect ~0.45.2), `npm view drizzle-kit version` (~0.31.10), `npm view @neondatabase/serverless version` (~1.1.0), `npm view jose version` (~6.2.3), `npm view bcryptjs version` (~3.0.3), `npm view zod version` (~4.4.3), `npm view @netlify/neon version` (NOT previously checked — record whatever it returns), `npm view vitest version`.

    2. Install runtime deps:
       `npm install drizzle-orm @neondatabase/serverless @netlify/neon jose bcryptjs zod`

    3. Install dev deps:
       `npm install -D drizzle-kit @types/bcryptjs vitest`

    4. Add a `"test"` script to package.json `scripts`: `"test": "vitest run"`. Do NOT use a watch flag (`vitest` without `run` defaults to watch mode — must be `vitest run` for CI/quick-run per VALIDATION.md "No watch-mode flags").

    5. Create `vitest.config.ts` at repo root:
       ```typescript
       import { defineConfig } from "vitest/config";

       export default defineConfig({
         test: {
           environment: "node",
           include: ["src/**/*.test.ts"],
           testTimeout: 15000,
         },
         resolve: {
           alias: { "@": new URL("./src", import.meta.url).pathname },
         },
       });
       ```
       The `@` alias must mirror tsconfig's existing `@/*` -> `./src/*` path mapping so test imports resolve identically to app code.

    6. Create a throwaway example test `src/lib/__smoke__.test.ts` containing `import { expect, test } from "vitest"; test("vitest runs", () => { expect(1 + 1).toBe(2); });` to prove the runner works. (This file will be deleted in a later plan once real tests exist — leave it for now as proof.)

    Record the actual installed `@netlify/neon` version into PROVISIONING-NOTES.md (created in Task 3).
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - package.json `dependencies` contains "drizzle-orm", "@neondatabase/serverless", "@netlify/neon", "jose", "bcryptjs", "zod"
    - package.json `devDependencies` contains "drizzle-kit", "@types/bcryptjs", "vitest"
    - package.json `scripts.test` equals "vitest run" (grep: `"test": "vitest run"`)
    - vitest.config.ts exists and contains `defineConfig`
    - `npm test` exits 0 with the smoke test passing
  </acceptance_criteria>
  <done>All phase dependencies installed at versions matching RESEARCH (or recorded if drifted), `npm test` runs vitest and passes the example test.</done>
</task>

<task type="auto">
  <name>Task 2: Provision Netlify Database and configure drizzle-kit migration output</name>
  <read_first>
    - netlify.toml (existing build config — confirms it-guru-online site, @netlify/plugin-nextjs plugin)
    - .planning/phases/01-auth-database-foundation/01-RESEARCH.md (Pattern 4 db client + drizzle.config.ts example; Open Questions 2/3/4 on @netlify/neon, `netlify database init` prompts, POPIA region)
    - CLAUDE.md (secrets-scoping discipline — the NEXT_PUBLIC_BASE_URL incident; new secrets must NOT use NEXT_PUBLIC_ prefix)
  </read_first>
  <action>
    Provision Netlify Database and wire drizzle-kit to feed Netlify's auto-apply migration runner (the chosen migration mechanism — do NOT also run `drizzle-kit migrate` independently; that is RESEARCH Pitfall 4 double-apply).

    1. Confirm Netlify CLI version >= 26.0.0: `netlify --version`. If below, `npm install -g netlify-cli`.

    2. Run `netlify database init` INTERACTIVELY (do NOT pass --yes — per RESEARCH Open Question 3, we need to observe what it scaffolds/installs and whether it exposes a region prompt). When prompted for query style, choose **Drizzle ORM**. Record in PROVISIONING-NOTES.md: (a) what packages it installed, (b) whether a region prompt appeared and what was selected (RESEARCH Open Question 4 / POPIA), (c) confirmation that `NETLIFY_DATABASE_URL` was created. If a region choice is offered, choose EU or equivalent-protection region per the POPIA decision in STATE.md; if none is offered, document "region non-configurable / inherited from site" explicitly.

    3. Verify the env var exists: `netlify env:list --site 2cb84145-76d9-4916-ae7d-9df49a5a348c` (or the it-guru-online site id) — confirm `NETLIFY_DATABASE_URL` is present and is NOT prefixed `NEXT_PUBLIC_`.

    4. Create `drizzle.config.ts` at repo root, exactly:
       ```typescript
       import { defineConfig } from "drizzle-kit";

       export default defineConfig({
         dialect: "postgresql",
         schema: "./src/lib/db/schema.ts",
         out: "./netlify/database/migrations",
         dbCredentials: {
           url: process.env.NETLIFY_DATABASE_URL!,
         },
       });
       ```
       (`schema.ts` is created in Plan 02 — pointing at a not-yet-existing path is fine for config.)

    5. Ensure `netlify/database/migrations/` exists and is git-tracked: create `netlify/database/migrations/.gitkeep` if `netlify database init` did not already create the directory.

    6. Add `JWT_SECRET`, `ADMIN_EMAIL`, `ADMIN_SEED_PASSWORD` placeholders to `.env.example` (create the file if absent) with comments — these are set manually by the owner/developer at deploy time per D-05. Use NON-public names (no NEXT_PUBLIC_ prefix). Example lines:
       ```
       # Set on Netlify (non-public scope) — NOT prefixed NEXT_PUBLIC_
       JWT_SECRET=               # 32+ random bytes, e.g. `openssl rand -base64 48`
       ADMIN_EMAIL=              # owner's chosen admin login email
       ADMIN_SEED_PASSWORD=      # owner's chosen plaintext password — used ONCE by the seed script, never stored
       # NETLIFY_DATABASE_URL is auto-injected by `netlify database init` — do not set here
       ```
  </action>
  <verify>
    <automated>node -e "require('fs').accessSync('drizzle.config.ts'); require('fs').accessSync('netlify/database/migrations'); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - drizzle.config.ts exists and contains `out: "./netlify/database/migrations"`
    - drizzle.config.ts contains `process.env.NETLIFY_DATABASE_URL`
    - `netlify/database/migrations/` directory exists and is git-tracked (contains .gitkeep or a scaffolded migration)
    - .env.example contains `JWT_SECRET=`, `ADMIN_EMAIL=`, `ADMIN_SEED_PASSWORD=`
    - .env.example does NOT contain the string `NEXT_PUBLIC_` on any of the three new secret lines
    - `netlify env:list` output (captured in PROVISIONING-NOTES.md) shows NETLIFY_DATABASE_URL present
  </acceptance_criteria>
  <done>Netlify Database provisioned, NETLIFY_DATABASE_URL injected, drizzle.config.ts points migration output at Netlify's auto-apply directory, secrets placeholders documented with correct (non-public) scoping.</done>
</task>

<task type="auto">
  <name>Task 3: Build proxy.ts smoke test and record provisioning answers</name>
  <read_first>
    - .planning/phases/01-auth-database-foundation/01-RESEARCH.md (Pattern 1 proxy.ts example + Open Question 1 smoke-test recommendation + Pitfall 2 file placement: src/proxy.ts NOT repo root)
    - src/app/layout.tsx (confirms src/app layout — proxy.ts belongs at src/proxy.ts, "same level as app")
  </read_first>
  <action>
    Create a MINIMAL no-op `proxy.ts` smoke test that contains NO auth logic — its only job is to prove the file fires under the real adapter (RESEARCH Open Question 1). Real auth logic goes into proxy.ts in Plan 03, replacing this smoke version.

    1. Create `src/proxy.ts` (NOTE: `src/proxy.ts`, NOT repo-root `./proxy.ts` — RESEARCH Pitfall 2; it must sit at the same level as `src/app/`):
       ```typescript
       import { NextResponse } from "next/server";
       import type { NextRequest } from "next/server";

       // SMOKE TEST ONLY (Plan 01). Replaced with real session-check logic in Plan 03.
       // Proves proxy.ts fires under @netlify/plugin-nextjs before real auth is built on it.
       export function proxy(request: NextRequest) {
         if (request.nextUrl.pathname === "/admin/smoke-test") {
           return NextResponse.redirect(new URL("/", request.url));
         }
         return NextResponse.next();
       }

       export const config = {
         matcher: ["/admin/:path*"],
       };
       ```

    2. Create `.planning/phases/01-auth-database-foundation/PROVISIONING-NOTES.md` recording answers to RESEARCH Open Questions 1-4:
       - **OQ1 (proxy.ts on Netlify):** to be filled by the human-verify checkpoint below — leave a "VERIFIED: <yes/no>, deploy URL: <url>" line.
       - **OQ2 (@netlify/neon version):** the version captured in Task 1.
       - **OQ3 (`netlify database init` behavior):** what it installed/scaffolded, captured in Task 2.
       - **OQ4 (POPIA region):** region prompt presence + choice, captured in Task 2.
       - Also record: installed `@netlify/plugin-nextjs` version (`npm list @netlify/plugin-nextjs`) — RESEARCH notes the repo is on 5.15.9 and official zero-config Next 16 support landed in 5.15.11+; note whether a bump is advisable.

    Note for deploy: the smoke test must be exercised on a REAL Netlify build (`next dev` does not run the adapter). Per CLAUDE.md, Netlify only deploys `main`, but deploy previews run on branch pushes — push the `dev` branch to trigger a deploy-preview build, OR follow the merge-to-main flow only if explicitly authorized. The checkpoint below tells the human exactly what to do.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/proxy.ts','utf8'); if(!s.includes('export const config')||!s.includes('matcher')) process.exit(1); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - src/proxy.ts exists (NOT ./proxy.ts at repo root)
    - src/proxy.ts contains `export const config` and `matcher: ["/admin/:path*"]`
    - src/proxy.ts contains the `/admin/smoke-test` redirect and NO import of jose/bcryptjs/db (it is a no-op smoke test only)
    - PROVISIONING-NOTES.md exists and contains the strings "OQ1", "OQ2", "OQ3", "OQ4"
  </acceptance_criteria>
  <done>Minimal proxy.ts smoke test created at the correct path, provisioning notes scaffolded with the four open-question answers (OQ1 pending the deploy verification checkpoint).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify proxy.ts fires on a real Netlify deploy</name>
  <what-built>
    A no-op proxy.ts smoke test that redirects /admin/smoke-test -> / , plus a provisioned Netlify Database and installed tooling. This checkpoint resolves RESEARCH Open Question 1 — the single highest-risk unknown in the phase — by confirming proxy.ts actually fires under @netlify/plugin-nextjs on a real build (local `next dev` cannot prove this).
  </what-built>
  <how-to-verify>
    1. Trigger a REAL Netlify build that includes src/proxy.ts. Either: push the current branch to trigger a Netlify deploy preview, OR (only if you explicitly authorize a production deploy) merge dev -> main per CLAUDE.md's branch-strategy flow and push main.
    2. Wait for the build to reach "ready" — confirm with `netlify api listSiteDeploys --data '{"site_id":"2cb84145-76d9-4916-ae7d-9df49a5a348c"}'` (look for state: "ready" on the latest deploy with the expected commit_ref).
    3. In a browser, visit `https://<deploy-url>/admin/smoke-test`.
    4. EXPECTED: you are redirected to the site root `/`. If you land on a 404 or stay on /admin/smoke-test, proxy.ts is NOT firing — that is a blocking finding that changes Plan 03's approach (page-level requireAdmin() becomes the sole boundary).
    5. Record the result in PROVISIONING-NOTES.md OQ1 line: "VERIFIED: yes/no, deploy URL: <url>".
  </how-to-verify>
  <resume-signal>Type "approved — proxy fires" if the redirect worked, or describe what happened (e.g. "404, no redirect") so Plan 03 can adjust to rely on page-level requireAdmin() instead.</resume-signal>
</task>

</tasks>

<verification>
- `npm test` passes (vitest installed and runnable)
- drizzle.config.ts points `out` at `./netlify/database/migrations`
- NETLIFY_DATABASE_URL confirmed present via `netlify env:list`
- proxy.ts smoke redirect verified on a real Netlify deploy (checkpoint)
- PROVISIONING-NOTES.md records answers to all four RESEARCH Open Questions
</verification>

<success_criteria>
- All Phase 1 dependencies installed (drizzle-orm, @neondatabase/serverless, @netlify/neon, jose, bcryptjs, zod, drizzle-kit, vitest, @types/bcryptjs)
- vitest adopted; `npm test` runs and passes an example test
- Netlify Database provisioned with NETLIFY_DATABASE_URL auto-injected (non-public scope)
- Single migration mechanism chosen: drizzle-kit generate -> netlify/database/migrations (Netlify auto-apply); no second migrate runner
- proxy.ts confirmed firing on a real Netlify build (RESEARCH Open Question 1 resolved from inferred to verified)
- POPIA region documented (STATE.md blocker satisfied)
</success_criteria>

<output>
After completion, create `.planning/phases/01-auth-database-foundation/01-01-SUMMARY.md`
</output>
