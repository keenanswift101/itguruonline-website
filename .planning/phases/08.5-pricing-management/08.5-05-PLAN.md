---
phase: 08.5-pricing-management
plan: 05
type: execute
wave: 3
depends_on: ["08.5-01", "08.5-03", "08.5-04"]
files_modified:
  - src/app/admin/pricing/AddonsTable.tsx
  - src/app/admin/pricing/page.tsx
  - src/app/(site)/services/page.tsx
autonomous: false
requirements: [PRICE-10]

must_haves:
  truths:
    - "A new AddonsTable renders each add-on as a read-only card by default; Edit → single Save Changes; Cancel discards (same flow as packages)"
    - "An 'Add Add-on' inline form creates an add-on (name, description, price, period) via POST"
    - "A Deactivate/Reactivate toggle hides/keeps an add-on; inactive dimmed in admin"
    - "The admin pricing page fetches getAllAddonsAdmin() and renders an Add-ons section"
    - "The public services page renders an Add-ons section from getAddons() (active only); renders nothing when empty"
    - "npx tsc --noEmit passes; npx vitest run is green"
  artifacts:
    - path: src/app/admin/pricing/AddonsTable.tsx
      provides: "Edit→Save-card admin CRUD for add-ons"
      contains: "isEditing"
      min_lines: 110
    - path: src/app/admin/pricing/page.tsx
      provides: "Add-ons admin section wired to getAllAddonsAdmin"
      contains: "getAllAddonsAdmin"
    - path: src/app/(site)/services/page.tsx
      provides: "public Add-ons section from getAddons"
      contains: "getAddons"
  key_links:
    - from: src/app/admin/pricing/AddonsTable.tsx
      to: /api/admin/pricing/addons
      via: "POST create + PATCH save/toggle via fetch"
      pattern: "/api/admin/pricing/addons"
    - from: src/app/(site)/services/page.tsx
      to: src/lib/pricing.ts
      via: "getAddons() active-only public read"
      pattern: "getAddons"
---

<objective>
Deliver PRICE-10's UI: a new `AddonsTable` admin component (Edit→Save-card + Add Add-on + Deactivate, mirroring PricingPackagesTable), an Add-ons section on `/admin/pricing` wired to `getAllAddonsAdmin()`, and a public Add-ons section on `/services` fed by `getAddons()` (active-only).

Purpose: Completes the third pricing entity end-to-end. Runs after plan 04 because it also edits `src/app/admin/pricing/page.tsx` (file-ownership sequencing) and consumes the addons routes from plan 03.

Output: AddonsTable.tsx, admin page addons section, public services add-ons section. Ends with a human-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08.5-pricing-management/08.5-RESEARCH.md
@src/app/admin/pricing/PricingPackagesTable.tsx
@src/app/admin/pricing/page.tsx
@src/app/(site)/services/page.tsx
@CLAUDE.md
</context>

<interfaces>
<!-- From @/lib/pricing (plan 01): -->
```typescript
interface AddonDTO { id; name; description; priceRands; pricePeriod: string; isActive: boolean; sortOrder; }
export async function getAddons(): Promise<AddonDTO[]>;          // PUBLIC — active only
export async function getAllAddonsAdmin(): Promise<AddonDTO[]>;  // ADMIN — all rows
```
<!-- Routes (plan 03):
  GET  /api/admin/pricing/addons -> { addons: [...] }
  POST /api/admin/pricing/addons { name, description, priceRands, pricePeriod } -> 201 { id }
  PATCH /api/admin/pricing/addons/[id] — full object OR { isActive } -->
<!-- pricePeriod is "once" | "mo" | "yr"; display: once -> flat R{n}, mo -> R{n}/month, yr -> R{n}/year. -->
<!-- IMPORTANT (Pitfall 5): this "addons" concept is UNRELATED to StepServiceSelection.tsx's hardcoded ADDONS checkbox array. Do NOT touch StepServiceSelection or clientRegistrations. -->
<!-- Public services section: mirror the existing "Hosting packages" <section> in services/page.tsx (Reveal + aria-label + max-w-5xl grid). Render nothing when addons is empty. -->
<!-- Reuse the PricingPackagesTable card pattern (isEditing/saving/useToast/Spinner/btn-metallic/btn-glass/scheme-dark select). -->
-->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create AddonsTable admin component (Edit→Save-card + Add Add-on + Deactivate)</name>
  <files>src/app/admin/pricing/AddonsTable.tsx</files>
  <read_first>src/app/admin/pricing/PricingPackagesTable.tsx (the plan-04 reworked version — copy the PackageCard + AddPackageForm structure), src/components/admin/AddScheduleForm.tsx (scheme-dark select for pricePeriod)</read_first>
  <action>
    Create `src/app/admin/pricing/AddonsTable.tsx` ("use client") closely mirroring the reworked PricingPackagesTable, but for AddonDTO and the addons routes:
    - `AddonCard({ addon }: { addon: AddonDTO })`: read-only card by default showing name, description, and price rendered as `R{priceRands}` + suffix (`""` for "once", `/month` for "mo", `/year` for "yr"); wrapper `opacity-50` when `!addon.isActive`. `Edit` reveals inputs bound to `fields` = `{ name, description, priceRands: String(...), pricePeriod }`. `Save Changes` PATCHes `/api/admin/pricing/addons/{addon.id}` with the FULL object `{ name, description, priceRands: Number(fields.priceRands), pricePeriod }`; toast.success("Add-on saved.") + router.refresh(). `Cancel` resets from props. `Deactivate`/`Reactivate` PATCHes `{ isActive: !addon.isActive }`.
    - pricePeriod editor: native <select> with scheme-dark + options "one-off" (value "once"), "per month" (value "mo"), "per year" (value "yr").
    - `AddAddonForm()`: `+ Add Add-on` toggles an inline form (name, description, price number, period select) + Create/Cancel. POST `/api/admin/pricing/addons`; on 201 toast.success("Add-on created.") + reset + close + router.refresh(); on 422 map `data.fields`; else toast.error.
    - Export `AddonsTable({ addons }: { addons: AddonDTO[] })`: render AddAddonForm then map to AddonCard. Empty-state: "No add-ons yet — add one above."
    - Imports: useState, useRouter, useToast, Spinner, AddonDTO from @/lib/pricing.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'const \[isEditing' src/app/admin/pricing/AddonsTable.tsx` matches
    - `grep -n 'AddonDTO' src/app/admin/pricing/AddonsTable.tsx` matches
    - `grep -n 'Add Add-on' src/app/admin/pricing/AddonsTable.tsx` matches
    - `grep -n 'Save Changes' src/app/admin/pricing/AddonsTable.tsx` matches
    - `grep -n 'Deactivate\|Reactivate' src/app/admin/pricing/AddonsTable.tsx` matches
    - `grep -n '/api/admin/pricing/addons' src/app/admin/pricing/AddonsTable.tsx` matches
    - `grep -n 'scheme-dark' src/app/admin/pricing/AddonsTable.tsx` matches
    - `grep -c 'onBlur' src/app/admin/pricing/AddonsTable.tsx` returns 0
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>AddonsTable renders read-only cards with Edit→Save-card, Add Add-on, Deactivate/Reactivate, useToast+Spinner, scheme-dark period select; no auto-save.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Add-ons section into admin page + public services page</name>
  <files>src/app/admin/pricing/page.tsx, src/app/(site)/services/page.tsx</files>
  <read_first>src/app/admin/pricing/page.tsx (plan-04 version with getAllHostingPackagesAdmin/getAllDomainPricesAdmin), src/app/(site)/services/page.tsx (lines 6, 135-136, 223-334 for the Hosting Packages section + Domain callout — insert add-ons between them), 08.5-RESEARCH.md "Public services page — add-ons section placement"</read_first>
  <action>
    admin page.tsx: import `getAllAddonsAdmin` from @/lib/pricing and `AddonsTable` from "./AddonsTable". Add `getAllAddonsAdmin()` to the Promise.all (destructure `addons`). Add a new `<section aria-label="Add-ons">` after the Domain prices section and before Site settings: an `<h2>Add-ons</h2>` + `<AddonsTable addons={addons} />`.

    public services page.tsx: change the import on line 6 to also import `getAddons` (`import { getHostingPackages, getAddons, getSiteSettings } from "@/lib/pricing"`). In ServicesPage(), fetch `const addons = await getAddons();` alongside packages. Insert a new Add-ons `<section aria-label="Add-on services">` AFTER the Hosting Packages section's closing tag and BEFORE the "Contact CTA" section, following the Research "Public services page — add-ons section placement" example: a Reveal-wrapped heading "Add-on Services" + a `grid gap-6 sm:grid-cols-2 lg:grid-cols-3` mapping `addons` to Reveal cards showing name/description/price (`R{a.priceRands}` + `/month`|`/year` suffix for mo|yr, nothing for once). Guard the whole section with `{addons.length > 0 && (...)}` so it renders nothing when empty. Match the page's dark card style (`border border-white/15 bg-white/8 backdrop-blur-sm`).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'getAllAddonsAdmin' src/app/admin/pricing/page.tsx` matches
    - `grep -n 'AddonsTable' src/app/admin/pricing/page.tsx` matches
    - `grep -n 'aria-label="Add-ons"' src/app/admin/pricing/page.tsx` matches
    - `grep -n 'getAddons' src/app/(site)/services/page.tsx` matches
    - `grep -n 'aria-label="Add-on services"' src/app/(site)/services/page.tsx` matches
    - `grep -n 'addons.length > 0' src/app/(site)/services/page.tsx` matches
    - npx tsc --noEmit exits 0; npx vitest run passes
  </acceptance_criteria>
  <done>admin page shows an Add-ons section (getAllAddonsAdmin + AddonsTable); public /services shows an active-only Add-ons section that hides when empty.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify add-ons CRUD and public /services add-ons section</name>
  <files>(manual verification — no files changed)</files>
  <what-built>Add-ons management end-to-end: /admin/pricing Add-ons section (Add / Edit→Save-card / Deactivate) and a public Add-ons section on /services fed by active-only add-ons.</what-built>
  <action>
    Human-verify checkpoint. Run `netlify dev` (with `DEV_AUTH_BYPASS=1`), ensure the 0009 migration is applied locally, and walk the steps in how-to-verify. Do NOT proceed to the SUMMARY until the owner types "approved".
  </action>
  <how-to-verify>
    1. Visit http://localhost:8888/admin/pricing — scroll to Add-ons. Click "+ Add Add-on", create e.g. "SSL Certificate / R150 / one-off" — confirm it appears as a read-only card.
    2. Confirm http://localhost:8888/services now shows an "Add-on Services" section with that add-on (price "R150", no period suffix for one-off).
    3. Edit the add-on price, Cancel (discarded), Edit again + Save Changes (one toast, card updates).
    4. Deactivate it — confirm it dims in admin and disappears from /services; Reactivate — confirm it returns.
    5. Deactivate ALL add-ons — confirm the /services Add-on Services section renders nothing (no empty heading).
  </how-to-verify>
  <verify>
    <manual>Owner walks steps 1-5 above under `netlify dev` and confirms each behaves as described.</manual>
  </verify>
  <done>Owner has confirmed add-on create, Edit→Save/Cancel, Deactivate/Reactivate, correct price-period suffixes, and the empty-state section hiding all work end-to-end.</done>
  <resume-signal>Type "approved" or describe issues (e.g. period suffix wrong, empty section still showing, inactive still public)</resume-signal>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — exits 0
2. `npx vitest run` — passes
3. `grep 'getAllAddonsAdmin\|AddonsTable' src/app/admin/pricing/page.tsx`
4. `grep 'getAddons\|aria-label="Add-on services"' src/app/(site)/services/page.tsx`
5. Human-verify checkpoint approved
</verification>

<success_criteria>
- AddonsTable: read-only cards → Edit → Save Changes, Add Add-on, Deactivate/Reactivate (no auto-save)
- Admin pricing page has an Add-ons section (unfiltered admin read)
- Public /services has an active-only Add-ons section that hides when empty
- StepServiceSelection.tsx untouched (unrelated ADDONS concept)
</success_criteria>

<output>
After completion, create `.planning/phases/08.5-pricing-management/08.5-05-SUMMARY.md`
</output>
