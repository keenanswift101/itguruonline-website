---
phase: 08.5-pricing-management
plan: 04
type: execute
wave: 2
depends_on: ["08.5-01", "08.5-02"]
files_modified:
  - src/app/admin/pricing/PricingPackagesTable.tsx
  - src/app/admin/pricing/DomainPricesTable.tsx
  - src/app/admin/pricing/page.tsx
autonomous: false
requirements: [PRICE-06, PRICE-07, PRICE-08, PRICE-09]

must_haves:
  truths:
    - "Each hosting package renders as a read-only card by default; an Edit button reveals editable fields; a single Save Changes button PATCHes ALL fields at once; Cancel discards (no request)"
    - "The per-field onBlur auto-save + SaveIndicator machinery is fully removed from both tables"
    - "An 'Add Package' inline form POSTs a new package and it appears after router.refresh()"
    - "A Deactivate/Reactivate button toggles isActive; inactive packages/TLDs render dimmed (opacity) in admin"
    - "Domain prices use the same Edit→Save-row flow + an 'Add TLD' form + a Deactivate toggle"
    - "The admin page reads getAllHostingPackagesAdmin() + getAllDomainPricesAdmin() (unfiltered) so inactive rows are visible/reactivatable"
    - "Save/create/toggle use useToast + Spinner; npx tsc --noEmit passes"
  artifacts:
    - path: src/app/admin/pricing/PricingPackagesTable.tsx
      provides: "Edit→Save-card packages UI + Add Package form + Deactivate"
      contains: "isEditing"
      min_lines: 120
    - path: src/app/admin/pricing/DomainPricesTable.tsx
      provides: "Edit→Save-row domains UI + Add TLD + Deactivate"
      contains: "isEditing"
    - path: src/app/admin/pricing/page.tsx
      provides: "admin reads repointed to unfiltered variants"
      contains: "getAllHostingPackagesAdmin"
  key_links:
    - from: src/app/admin/pricing/page.tsx
      to: src/lib/pricing.ts
      via: "getAllHostingPackagesAdmin + getAllDomainPricesAdmin (admin, unfiltered)"
      pattern: "getAllHostingPackagesAdmin"
    - from: src/app/admin/pricing/PricingPackagesTable.tsx
      to: /api/admin/pricing/packages
      via: "POST create + PATCH save/toggle via fetch"
      pattern: "/api/admin/pricing/packages"
---

<objective>
Rework the packages + domains admin UI from per-field auto-save to the explicit Edit → Save-card flow (PRICE-07/09), add inline create forms (PRICE-06 packages / PRICE-09 domains), and add Deactivate/Reactivate toggles (PRICE-08). Repoint the admin page to the unfiltered admin reads so inactive rows stay visible and reactivatable.

Purpose: This is the owner-facing half of packages + domains management. It retires the accidental-auto-save UX the owner explicitly asked to remove, and consumes the routes built in plan 02.

Output: reworked PricingPackagesTable.tsx + DomainPricesTable.tsx, repointed page.tsx. Ends with a human-verify checkpoint (visual UX + live-DB behavior).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08.5-pricing-management/08.5-RESEARCH.md
@src/app/admin/pricing/PricingPackagesTable.tsx
@src/app/admin/pricing/DomainPricesTable.tsx
@src/app/admin/pricing/page.tsx
@src/components/forms/ClientForm.tsx
@src/components/admin/AddScheduleForm.tsx
@CLAUDE.md
</context>

<interfaces>
<!-- DTOs from @/lib/pricing (plan 01): -->
```typescript
interface HostingPackageDTO { id; slug; name; priceRands; pricePeriod; description; features: string[]; isPopular; sortOrder; isActive: boolean; }
interface DomainPriceAdminRow { tld: string; priceRands: number | null; isActive: boolean; }
export async function getAllHostingPackagesAdmin(): Promise<HostingPackageDTO[]>;
export async function getAllDomainPricesAdmin(): Promise<DomainPriceAdminRow[]>;
```
<!-- Routes (plan 02):
  POST /api/admin/pricing/packages { name, priceRands, pricePeriod, description, features, isPopular } -> 201 { id }
  PATCH /api/admin/pricing/packages/[id] — full object OR { isActive }
  POST /api/admin/pricing/domains { tld, priceRands } -> 201 { tld }
  PATCH /api/admin/pricing/domains/[tld] — { priceRands } OR { isActive } -->
<!-- useToast from @/components/ui/Toast (toast.success/toast.error); Spinner from @/components/ui/Spinner. -->
<!-- Tailwind v4: text-(--text-secondary), bg-(--bg-primary), border-(--border-color). Buttons: btn-metallic (primary), btn-glass (secondary). -->
<!-- Native <select> for pricePeriod needs scheme-dark: className includes "scheme-dark" + style={{ colorScheme: "dark" }} + option className bg-(--bg-primary) text-(--text-primary) (see AddScheduleForm.tsx). -->
<!-- 422 responses shape: { error, fields: { <field>: [msg] } }. -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rework PricingPackagesTable to Edit→Save-card + Add Package + Deactivate</name>
  <files>src/app/admin/pricing/PricingPackagesTable.tsx</files>
  <read_first>src/app/admin/pricing/PricingPackagesTable.tsx (full — retire ALL of: SaveState type, patch(), SaveIndicator, per-field onBlur save/saveIsPopular, saveStates), 08.5-RESEARCH.md "Pattern 1", src/components/forms/ClientForm.tsx (useToast+Spinner+field errors), src/components/admin/AddScheduleForm.tsx (scheme-dark select)</read_first>
  <action>
    Fully rewrite the file. DELETE the `SaveState` type, `patch()`, `SaveIndicator`, and all `onBlur`/`saveStates`/`saveIsPopular` code — the per-field auto-save is retired entirely (do not layer on top of it).

    Build a `PackageCard({ pkg }: { pkg: HostingPackageDTO })` component per Research Pattern 1:
    - `useRouter()`, `useToast()`, `useState` for `isEditing` (false), `fields` (from `toEditable(pkg)`), and `saving` (false).
    - `toEditable(pkg)` returns `{ name, priceRands: String(pkg.priceRands), pricePeriod, description, features: pkg.features.join("\n"), isPopular }`.
    - READ-ONLY view (default): show name, `R{priceRands}/{pricePeriod === "mo" ? "month" : "year"}`, description, features list, a "Most Popular" badge if isPopular. Card wrapper `opacity-50` when `!pkg.isActive`. Footer buttons: `Edit` (btn-glass, sets fields=toEditable then isEditing=true) and `Deactivate`/`Reactivate` (border button calling toggleActive).
    - EDIT view: inputs bound to `fields` (name text, priceRands number, pricePeriod native <select> with scheme-dark + options "per month"/"per year", description text, features textarea, isPopular checkbox) — NO onBlur. Footer: `Save Changes` (btn-metallic, disabled while saving, shows <Spinner/> Saving…) and `Cancel` (btn-glass, resets fields=toEditable(pkg), isEditing=false).
    - `save()`: `setSaving(true)`, `fetch('/api/admin/pricing/packages/'+pkg.id, { method:"PATCH", headers, body: JSON.stringify({ name, priceRands: Number(fields.priceRands), pricePeriod, description, features: fields.features, isPopular }) })` — ALWAYS send the full field set (Pitfall 4). On res.ok: toast.success("Package saved."), setIsEditing(false), router.refresh(). Else toast.error("Save failed — check the fields and try again."). Finally setSaving(false).
    - `toggleActive()`: PATCH `{ isActive: !pkg.isActive }`; on ok toast.success(pkg.isActive ? "Deactivated." : "Reactivated.") + router.refresh(); else toast.error("Action failed.").

    Build an `AddPackageForm()` component (inline, collapsible): a `+ Add Package` btn-glass toggles an open boolean; when open, render name/priceRands/pricePeriod/description/features/isPopular inputs bound to local state + a `Create` (btn-metallic, Spinner while submitting) and `Cancel`. On submit POST `/api/admin/pricing/packages`; on 201 toast.success("Package created.") + reset + close + router.refresh(); on 422 map `data.fields` to inline errors; else toast.error. Slug is derived server-side — do NOT expose a slug input.

    Export `PricingPackagesTable({ packages }: { packages: HostingPackageDTO[] })`: render `<AddPackageForm/>` then map packages to `<PackageCard/>`. Keep the empty-state text but reword to "No packages yet — add one above."

    "use client" stays at the top. Import useToast, Spinner, useRouter, useState, HostingPackageDTO.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'onBlur' src/app/admin/pricing/PricingPackagesTable.tsx` returns 0
    - `grep -c 'SaveIndicator\|saveStates' src/app/admin/pricing/PricingPackagesTable.tsx` returns 0
    - `grep -n 'const \[isEditing' src/app/admin/pricing/PricingPackagesTable.tsx` matches
    - `grep -n 'useToast' src/app/admin/pricing/PricingPackagesTable.tsx` matches
    - `grep -n 'Save Changes' src/app/admin/pricing/PricingPackagesTable.tsx` matches
    - `grep -n 'Add Package' src/app/admin/pricing/PricingPackagesTable.tsx` matches
    - `grep -n 'isActive ? "Deactivate"\|Reactivate' src/app/admin/pricing/PricingPackagesTable.tsx` matches
    - `grep -n 'scheme-dark' src/app/admin/pricing/PricingPackagesTable.tsx` matches
    - `grep -n "method: \"POST\"" src/app/admin/pricing/PricingPackagesTable.tsx` matches
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>PricingPackagesTable is a read-only-card → Edit → single Save Changes flow with Add Package + Deactivate/Reactivate, useToast+Spinner, scheme-dark select; no onBlur/SaveIndicator remains.</done>
</task>

<task type="auto">
  <name>Task 2: Rework DomainPricesTable to Edit→Save-row + Add TLD + Deactivate, and repoint page.tsx to admin reads</name>
  <files>src/app/admin/pricing/DomainPricesTable.tsx, src/app/admin/pricing/page.tsx</files>
  <read_first>src/app/admin/pricing/DomainPricesTable.tsx (full — retire SaveState/patch/SaveIndicator/onBlur), src/app/admin/pricing/page.tsx (current getHostingPackages/getDomainPriceMap usage + copy), 08.5-RESEARCH.md "Pattern 1" + "Pitfall 2"</read_first>
  <action>
    DomainPricesTable rewrite — prop shape CHANGES from `Record<string, number|null>` to `DomainPriceAdminRow[]` (import the type from @/lib/pricing). Retire SaveState/patch/SaveIndicator/onBlur entirely.
    - `DomainRow({ row }: { row: DomainPriceAdminRow })`: read-only by default showing tld (mono) + `R{priceRands ?? "—"}` + a dimmed row (opacity-50) when `!row.isActive`. `Edit` reveals a number input bound to local `price` state (blank = null) + `Save`/`Cancel`. `Save` PATCHes `/api/admin/pricing/domains/{encodeURIComponent(row.tld)}` with `{ priceRands: price.trim() === "" ? null : Number(price) }`; toast + router.refresh(). A `Deactivate`/`Reactivate` button PATCHes `{ isActive: !row.isActive }`.
    - `AddTldForm()`: `+ Add TLD` toggles an inline form with a tld text input (placeholder ".co.za") + optional price number input + Create/Cancel. POST `/api/admin/pricing/domains` `{ tld, priceRands: price ? Number(price) : null }`; on 201 toast.success + reset + close + router.refresh(); on 422 show inline error from `data.fields.tld`; else toast.error.
    - Export `DomainPricesTable({ rows }: { rows: DomainPriceAdminRow[] })`: render AddTldForm + a table (TLD / Price / Status(active|inactive) / actions) mapping rows to DomainRow. Keep useToast + Spinner. Empty-state: "No domain prices yet — add one above."

    page.tsx repoint (the is_active caution): import `getAllHostingPackagesAdmin, getAllDomainPricesAdmin` (drop `getDomainPriceMap` import here) and call them in the Promise.all. Pass `packages={packages}` (unchanged prop name) and change DomainPricesTable prop to `rows={domainRows}`. Update the header copy line from "Edits auto-save on blur…" to "Edit a card, then Save Changes to publish. Deactivated items are hidden from the public site but kept here." Keep the SiteSettingsForm section untouched.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'onBlur' src/app/admin/pricing/DomainPricesTable.tsx` returns 0
    - `grep -n 'DomainPriceAdminRow' src/app/admin/pricing/DomainPricesTable.tsx` matches
    - `grep -n 'Add TLD' src/app/admin/pricing/DomainPricesTable.tsx` matches
    - `grep -n 'Deactivate\|Reactivate' src/app/admin/pricing/DomainPricesTable.tsx` matches
    - `grep -n 'getAllHostingPackagesAdmin' src/app/admin/pricing/page.tsx` matches
    - `grep -n 'getAllDomainPricesAdmin' src/app/admin/pricing/page.tsx` matches
    - `grep -c 'getDomainPriceMap' src/app/admin/pricing/page.tsx` returns 0
    - `grep -n 'rows={' src/app/admin/pricing/page.tsx` matches
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>DomainPricesTable is an Edit→Save-row flow with Add TLD + Deactivate consuming DomainPriceAdminRow[]; admin page reads the unfiltered admin variants; copy updated; tsc clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify packages + domains Edit→Save-card UX and live public reflection</name>
  <files>(manual verification — no files changed)</files>
  <what-built>Reworked /admin/pricing packages + domains management: read-only cards, Edit → single Save Changes (no auto-save), Add Package / Add TLD inline forms, Deactivate/Reactivate toggles. Public reads now filter inactive rows.</what-built>
  <action>
    Human-verify checkpoint. Run `netlify dev` (with `DEV_AUTH_BYPASS=1` in `.env.local`) and, after applying the 0009 migration to the local DB (`npm run db:migrate` if the local DB lacks is_active/addons), walk the steps in how-to-verify. Do NOT proceed to the SUMMARY until the owner types "approved".
  </action>
  <how-to-verify>
    1. Visit http://localhost:8888/admin/pricing — confirm each package is a read-only CARD (not editable inputs) by default.
    2. Click Edit on a package, change the price, click Cancel — confirm the change is discarded (no save). Click Edit again, change price + a feature, click Save Changes — confirm ONE toast "Package saved." and the card updates.
    3. Click "+ Add Package", fill name/price, Create — confirm it appears in the list and (in a second tab) on http://localhost:8888/services within a refresh.
    4. Deactivate a package — confirm it dims in admin and DISAPPEARS from /services; Reactivate — confirm it returns.
    5. Repeat 2-4 for a domain TLD (Add TLD, edit price, deactivate) and confirm the TLD reflects on /domain-checker.
    6. Confirm NO "Saved ✓" auto-save indicator appears while merely typing/tabbing between fields.
  </how-to-verify>
  <verify>
    <manual>Owner walks steps 1-6 above under `netlify dev` and confirms each behaves as described.</manual>
  </verify>
  <done>Owner has confirmed read-only cards, Edit→Save/Cancel (no auto-save), Add Package/Add TLD, and Deactivate/Reactivate (hidden on public, kept in admin) all work end-to-end.</done>
  <resume-signal>Type "approved" or describe issues (e.g. auto-save still firing, inactive still public, toast missing)</resume-signal>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — exits 0
2. `grep -c 'onBlur' src/app/admin/pricing/PricingPackagesTable.tsx src/app/admin/pricing/DomainPricesTable.tsx` — both 0
3. `grep 'getAllHostingPackagesAdmin\|getAllDomainPricesAdmin' src/app/admin/pricing/page.tsx`
4. Human-verify checkpoint approved
</verification>

<success_criteria>
- Read-only cards → Edit → single Save Changes; Cancel discards; no per-field auto-save anywhere
- Add Package + Add TLD create forms POST and refresh live
- Deactivate/Reactivate toggles isActive; inactive dimmed in admin, hidden on public
- Admin page reads unfiltered admin variants
</success_criteria>

<output>
After completion, create `.planning/phases/08.5-pricing-management/08.5-04-SUMMARY.md`
</output>
