---
phase: 10-quotations
plan: 06
type: execute
wave: 4
depends_on: [04, 05]
files_modified:
  - src/components/forms/QuotationStatusActions.tsx
  - src/app/admin/quotations/[id]/page.tsx
autonomous: false
requirements: [QUOTE-02, QUOTE-04, QUOTE-05]

must_haves:
  truths:
    - "QuotationStatusActions drives the full lifecycle: draft shows Mark Sent + Delete; sent shows Mark Accepted + Mark Declined + Resend + Revert to Draft; declined shows Mark Sent; accepted shows Convert to Invoice (or a View Invoice link once converted)"
    - "Mark Sent surfaces the 422 no_client_email prompt (add a client email first); Convert POSTs to /convert and redirects to the new draft invoice; a 409 on convert shows 'already converted' and refreshes"
    - "The detail page renders QuotationForm (edit) when status is draft, and a read-only view otherwise; it shows the status badge (+ Expired), a Download PDF link, and QuotationStatusActions"
    - "npx tsc --noEmit passes"
  artifacts:
    - path: src/components/forms/QuotationStatusActions.tsx
      provides: "lifecycle + convert buttons (QUOTE-02/04/05)"
      contains: "Convert to Invoice"
    - path: src/app/admin/quotations/[id]/page.tsx
      provides: "quotation detail/edit page"
      contains: "QuotationStatusActions"
  key_links:
    - from: src/components/forms/QuotationStatusActions.tsx
      to: src/app/api/admin/quotations/[id]/convert/route.ts
      via: "fetch POST /convert then redirect to the new invoice"
      pattern: "/convert"
    - from: src/app/admin/quotations/[id]/page.tsx
      to: src/components/forms/QuotationForm.tsx
      via: "renders the edit form when status is draft"
      pattern: "QuotationForm"
---

<objective>
Build the quotation detail experience: `QuotationStatusActions.tsx` (mirrors `InvoiceStatusActions` but for the draft→sent→accepted/declined lifecycle, plus a Convert-to-Invoice button on accepted quotations and a Resend/Revert set on sent ones), and the quotation detail page (`[id]/page.tsx`) that edits a draft via `QuotationForm` or shows a read-only view, with status actions + Download PDF.

Purpose: This completes QUOTE-02 (edit/delete draft from the detail page), QUOTE-04 (status lifecycle UI), and QUOTE-05 (one-click convert button). Depends on 10-04 (status/resend/convert routes) and 10-05 (QuotationForm). This is the phase's final wave. It ends with a human-verify checkpoint covering the full lifecycle + email + convert.

Output: `QuotationStatusActions.tsx`, `[id]/page.tsx`, and a lifecycle verification checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-quotations/10-RESEARCH.md
@CLAUDE.md
@src/components/forms/InvoiceStatusActions.tsx
@src/app/admin/invoices/[id]/page.tsx
@src/components/forms/QuotationForm.tsx
@src/lib/quotation-status.ts
@src/lib/quotations.ts
</context>

<interfaces>
<!-- Created by 10-01/10-04/10-05 (depends_on: 04, 05): -->
- PATCH /api/admin/quotations/[id]/status { status } → 200 { ok } | 409 invalid | 422 { error: "no_client_email" }.
- POST /api/admin/quotations/[id]/resend → 200 { ok } | 409 | 422 no_client_email.
- POST /api/admin/quotations/[id]/convert → 201 { id } (new invoice id) | 409 (already converted / not accepted) | 404.
- QuotationForm from "@/components/forms/QuotationForm" (props quotationId, initial, clients, initialClientId).
- getClientsForPicker() from "@/lib/client-query"; STATUS_BADGE/EXPIRED_BADGE/isExpired from "@/lib/quotation-status"; formatQuotationNumber from "@/lib/quotations".
- quotations row has `convertedInvoiceId: number | null` — pass it to the actions component to toggle Convert vs View Invoice.

<!-- Templates to mirror: -->
- QuotationStatusActions ← InvoiceStatusActions.tsx (same patch()/resend()/del() shape + error handling; add a convert() handler and a per-status button set).
- detail page ← src/app/admin/invoices/[id]/page.tsx (draft → QuotationForm edit; else read-only; badge; Download PDF; status actions).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: QuotationStatusActions.tsx (lifecycle + convert)</name>
  <files>src/components/forms/QuotationStatusActions.tsx</files>
  <read_first>src/components/forms/InvoiceStatusActions.tsx (full — template for patch/resend/del + error UI), src/lib/quotation-status.ts (ALLOWED_TRANSITIONS shape)</read_first>
  <action>
    Create `src/components/forms/QuotationStatusActions.tsx` (`"use client"`) by adapting InvoiceStatusActions:
    - Props: `{ id: number; status: string; convertedInvoiceId: number | null }`.
    - Keep `patch(target)` (PATCH /api/admin/quotations/${id}/status) with the same 409 + 422 `no_client_email` handling — reword the no-email message to "This quotation has no client email. Add a client email (edit the draft above) before marking it Sent."
    - Keep `resend()` (POST /api/admin/quotations/${id}/resend) with 422 no_client_email handling.
    - Keep `del()` (DELETE /api/admin/quotations/${id}) with the confirm() + redirect to /admin/quotations.
    - Add `convert()`: POST `/api/admin/quotations/${id}/convert`; on `res.status === 201` read `{ id }` and `router.push(\`/admin/invoices/${id}\`)`; on 409 set error "This quotation has already been converted." and `router.refresh()`; else generic error.
    - Button sets by status:
      - `draft`: "Mark Sent" (`patch("sent")`, btn-metallic) + "Delete" (`del`, btn-glass red).
      - `sent`: "Mark Accepted" (`patch("accepted")`, btn-metallic) + "Mark Declined" (`patch("declined")`, btn-glass) + "Resend" (`resend`, btn-glass) + "Revert to Draft" (`patch("draft")`, btn-glass).
      - `declined`: "Mark Sent" (`patch("sent")`, btn-glass).
      - `accepted`: if `convertedInvoiceId == null` → "Convert to Invoice" (`convert`, btn-metallic); else a static "View Invoice" `<Link>` → `/admin/invoices/${convertedInvoiceId}` (btn-glass, no fetch).
    - Keep the `pending` guard + `role="alert"` error paragraph exactly like InvoiceStatusActions.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'Convert to Invoice' src/components/forms/QuotationStatusActions.tsx` matches
    - `grep -n '/api/admin/quotations/\${id}/convert' src/components/forms/QuotationStatusActions.tsx` matches
    - `grep -n 'router.push(`/admin/invoices/' src/components/forms/QuotationStatusActions.tsx` matches
    - `grep -n 'Mark Accepted' src/components/forms/QuotationStatusActions.tsx` matches
    - `grep -n 'Mark Declined' src/components/forms/QuotationStatusActions.tsx` matches
    - `grep -n 'convertedInvoiceId' src/components/forms/QuotationStatusActions.tsx` matches
    - `grep -n 'no_client_email' src/components/forms/QuotationStatusActions.tsx` matches
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>QuotationStatusActions renders the correct button set per status, handles the no-email prompt, and converts an accepted quote to a draft invoice (redirect) or links to the already-converted invoice.</done>
</task>

<task type="auto">
  <name>Task 2: Quotation detail/edit page ([id]/page.tsx)</name>
  <files>src/app/admin/quotations/[id]/page.tsx</files>
  <read_first>src/app/admin/invoices/[id]/page.tsx (full template), src/components/forms/QuotationForm.tsx, src/components/forms/QuotationStatusActions.tsx</read_first>
  <action>
    Create `src/app/admin/quotations/[id]/page.tsx` mirroring the invoice detail page:
    - requireAdmin (redirect /admin/login); parse id (notFound on NaN); select the quotation (notFound if missing); select line items ordered by sortOrder, id; `const clients = await getClientsForPicker();`.
    - Compute `badge = STATUS_BADGE[q.status] ?? STATUS_BADGE.draft`, `expired = isExpired(q.status, q.validUntil)`, `reference = formatQuotationNumber(q.id)`.
    - Header: back link → /admin/quotations; `<h1>{reference}</h1>`; badge (+ Expired badge); `<QuotationStatusActions id={q.id} status={q.status} convertedInvoiceId={q.convertedInvoiceId} />`; a "Download PDF" anchor → `/api/admin/quotations/${q.id}/pdf`.
    - Body card: if `q.status === "draft"` render `<QuotationForm quotationId={q.id} clients={clients} initialClientId={q.clientId} initial={{ clientName, clientEmail ?? "", billingAddress ?? "", issueDate, validUntil, lineItems: [...] }} />`; else render the read-only view (Client Name/Email, Billing Address, Issue Date, **Valid Until**, line-items table, Quotation Total) — copy the invoice read-only block, replacing Due Date with Valid Until and "Invoice Total" with "Quotation Total".
    - `export const metadata = { title: "Quotation" }`, `export const dynamic = "force-dynamic"`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'QuotationStatusActions' src/app/admin/quotations/[id]/page.tsx` matches
    - `grep -n 'convertedInvoiceId={q.convertedInvoiceId}' src/app/admin/quotations/[id]/page.tsx` matches
    - `grep -n 'q.status === "draft"' src/app/admin/quotations/[id]/page.tsx` matches
    - `grep -n 'Valid Until' src/app/admin/quotations/[id]/page.tsx` matches
    - `grep -n '/api/admin/quotations/\${q.id}/pdf' src/app/admin/quotations/[id]/page.tsx` matches
    - `grep -c 'Due Date' src/app/admin/quotations/[id]/page.tsx` returns 0
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>The detail page edits a draft via QuotationForm, shows a read-only view otherwise, and exposes the badge, Download PDF, and status/convert actions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify the full quotation lifecycle end-to-end</name>
  <files>(manual verification — no files changed)</files>
  <what-built>The complete quotations feature: create/edit/delete, PDF, email-on-send, status lifecycle (draft→sent→accepted/declined), and convert-to-invoice — all wired through the admin UI.</what-built>
  <action>
    Human-verify checkpoint. Run `netlify dev` (with `DEV_AUTH_BYPASS=1` in `.env.local`) and walk the full lifecycle in the admin portal (see how-to-verify). Do NOT proceed to the SUMMARY until the owner types "approved".
  </action>
  <how-to-verify>
    In the admin portal:
    1. Go to /admin/quotations → "New Quotation". Pick a stored client (confirm name/email/address auto-fill), add 2 line items, set a Valid Until date, Create. Confirm redirect to the detail page with a QUO-#### reference.
    2. On the detail page (draft): edit a line item + Save; confirm it persists. Click "Download PDF" — confirm the PDF is labeled "Quotation", shows the QUO-#### reference + Valid Until, has a validity/terms footer (NO bank details, NO invoice number).
    3. Remove the client email (edit) and click "Mark Sent" — confirm the 422 prompt to add a client email. Re-add the email, Mark Sent — confirm the status becomes Sent and the quotation PDF arrives by email (check the test inbox + the info@it-guru.co.za BCC).
    4. Click "Resend" — confirm a second email arrives with no status change.
    5. Click "Mark Accepted", then "Convert to Invoice" — confirm redirect to a NEW draft invoice carrying the same client + line items (30-day due date). Return to the quotation and confirm it now shows "View Invoice" (not Convert), and clicking Convert again is blocked (already converted).
    6. On another quotation, exercise "Mark Declined" then "Mark Sent" (re-open). Confirm the list page filters (All/Draft/Sent/Accepted/Declined) and shows an Expired badge on a sent quote whose Valid Until is in the past.
  </how-to-verify>
  <verify>
    <manual>Owner walks steps 1-6 above under `netlify dev` and confirms each behaves as described.</manual>
  </verify>
  <done>Owner has confirmed create, edit, PDF, email-on-send + no-email prompt, resend, accept/decline, convert (+ re-convert blocked), and list filters/Expired badge all work end-to-end.</done>
  <resume-signal>Type "approved" or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — exits 0
2. `npx vitest run` — full suite green (DB tests skipped without NETLIFY_DB_URL)
3. `grep 'Convert to Invoice' src/components/forms/QuotationStatusActions.tsx`
4. `grep 'QuotationStatusActions' src/app/admin/quotations/[id]/page.tsx`
5. Human-verify checkpoint (Task 3) approved.
</verification>

<success_criteria>
- QuotationStatusActions drives the full lifecycle + convert
- Detail page edits a draft / shows read-only otherwise, with badge, PDF, and actions
- Convert redirects to a new draft invoice; re-convert blocked
- Lifecycle checkpoint approved end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/10-quotations/10-06-SUMMARY.md`
</output>
