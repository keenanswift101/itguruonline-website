---
phase: 10-quotations
plan: 05
type: execute
wave: 3
depends_on: [01, 03]
files_modified:
  - src/components/forms/QuotationForm.tsx
  - src/app/admin/quotations/new/page.tsx
  - src/app/admin/quotations/page.tsx
  - src/components/admin/AdminSidebar.tsx
autonomous: true
requirements: [QUOTE-01, QUOTE-06]

must_haves:
  truths:
    - "QuotationForm mirrors InvoiceForm: reuses the existing ClientPicker (auto-fills name/email/address), has a required 'Valid Until' date field (replacing invoice 'Due Date'), and POSTs to /api/admin/quotations (or PUT /api/admin/quotations/[id] in edit mode)"
    - "The New Quotation page renders QuotationForm with getClientsForPicker() clients and requireAdmin() gating"
    - "The Quotations list page lists all quotations newest-first, filters by draft/sent/accepted/declined via ?status=, shows a status badge (+ Expired badge when isExpired), and a Download PDF link per row"
    - "AdminSidebar has a Quotations nav entry between Invoices and Automations"
    - "npx tsc --noEmit passes"
  artifacts:
    - path: src/components/forms/QuotationForm.tsx
      provides: "create/edit quotation form reusing ClientPicker (QUOTE-01)"
      contains: "validUntil"
    - path: src/app/admin/quotations/page.tsx
      provides: "quotations list + status filter + PDF download link (QUOTE-06)"
      contains: "FILTERABLE_STATUSES"
    - path: src/app/admin/quotations/new/page.tsx
      provides: "new quotation page"
      contains: "QuotationForm"
    - path: src/components/admin/AdminSidebar.tsx
      provides: "Quotations nav entry"
      contains: "/admin/quotations"
  key_links:
    - from: src/components/forms/QuotationForm.tsx
      to: src/app/api/admin/quotations/route.ts
      via: "fetch POST/PUT on submit"
      pattern: "fetch\\(.*api/admin/quotations"
    - from: src/app/admin/quotations/page.tsx
      to: src/app/api/admin/quotations/[id]/pdf/route.tsx
      via: "Download PDF link per row"
      pattern: "/pdf"
---

<objective>
Build the quotation creation + listing UI: `QuotationForm.tsx` (mirrors `InvoiceForm`, reuses the existing `ClientPicker` unmodified, swaps Due Date → Valid Until), the New Quotation page, the Quotations list page (status filter + per-row Download PDF link), and the AdminSidebar Quotations nav entry.

Purpose: This delivers the create half of QUOTE-01 (form + new page) and QUOTE-06 (list + filter + download). Depends on 10-01 (quotationInput contract, quotation-status badges) and 10-03 (POST/PUT routes the form calls). The detail/edit page + status actions land in 10-06.

Output: `QuotationForm.tsx`, `new/page.tsx`, `page.tsx` (list), and the sidebar edit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-quotations/10-RESEARCH.md
@CLAUDE.md
@src/components/forms/InvoiceForm.tsx
@src/components/forms/ClientPicker.tsx
@src/app/admin/invoices/new/page.tsx
@src/app/admin/invoices/page.tsx
@src/components/admin/AdminSidebar.tsx
@src/lib/quotation-status.ts
@src/lib/client-query.ts
</context>

<interfaces>
<!-- Created by 10-01/10-03 (depends_on: 01, 03): -->
- POST /api/admin/quotations expects { clientId?, clientName, clientEmail, billingAddress, issueDate, validUntil, lineItems: [{description, quantity, unitPriceRands}] } → 201 { ok, id }.
- PUT /api/admin/quotations/[id] same body → 200 { ok } (draft only).
- STATUS_BADGE, EXPIRED_BADGE, isExpired from "@/lib/quotation-status".
- getClientsForPicker() from "@/lib/client-query" (EXISTING, unmodified) → ClientPickerOption[].
- ClientPicker from "@/components/forms/ClientPicker" (EXISTING, unmodified) — props { clients, selectedClientId, onSelect }.

<!-- Templates to mirror: -->
- QuotationForm ← InvoiceForm.tsx (rename Due Date → Valid Until; on submit hit /api/admin/quotations; on 201 redirect to /admin/quotations/[id]; on 422 map fields incl. validUntil; keep clientId only set in handleClientSelect).
- list page ← src/app/admin/invoices/page.tsx (FILTERABLE_STATUSES = draft/sent/accepted/declined; reference column = formatQuotationNumber(row.id); Valid Until column; add a Download PDF anchor per row → /api/admin/quotations/{id}/pdf).
- new page ← src/app/admin/invoices/new/page.tsx.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: QuotationForm.tsx (create/edit, reuses ClientPicker, Valid Until field)</name>
  <files>src/components/forms/QuotationForm.tsx</files>
  <read_first>src/components/forms/InvoiceForm.tsx (full — this is the template), src/components/forms/ClientPicker.tsx (props)</read_first>
  <action>
    Create `src/components/forms/QuotationForm.tsx` by copying `InvoiceForm.tsx` and adapting:
    - Rename the component to `QuotationForm`, props interface to `QuotationFormProps` with `quotationId?`, `initial?`, `clients?`, `initialClientId?`.
    - In `ClientFields`/`FieldErrors`/`initialClientFields`, rename `dueDate` → `validUntil` throughout (label "Valid Until", still required — keep the `* required` marker and the `validate()` check `if (!fields.validUntil.trim()) errs.validUntil = "Valid until date is required.";`).
    - In the submit body, send `validUntil` instead of `dueDate` (keep `issueDate`, `clientId`, `clientName`, `clientEmail`, `billingAddress`, `lineItems`).
    - Point fetch at `/api/admin/quotations` (POST) or `/api/admin/quotations/${quotationId}` (PUT). On 201 → `router.push(\`/admin/quotations/${data.id}\`)`. Keep the edit-mode `router.refresh()` path.
    - Keep `handleClientSelect` setting `clientId` (never in `handleFieldChange`) — auto-fill name/email/billingAddress exactly as InvoiceForm does.
    - Change button labels: "Create Quotation" / "Creating…" / "Save Changes" / "Saving…". Change the total label to "Quotation Total:".
    - Reuse `ClientPicker` unmodified (import from "@/components/forms/ClientPicker"). Tailwind v4 canonical tokens (text-(--text-primary) etc.) — copy verbatim, do NOT introduce `text-[var(--...)]`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'export default function QuotationForm' src/components/forms/QuotationForm.tsx` matches
    - `grep -n 'validUntil' src/components/forms/QuotationForm.tsx` matches
    - `grep -c 'dueDate' src/components/forms/QuotationForm.tsx` returns 0
    - `grep -n 'fetch("/api/admin/quotations"' src/components/forms/QuotationForm.tsx` matches
    - `grep -n 'ClientPicker' src/components/forms/QuotationForm.tsx` matches
    - `grep -c 'text-\[var(' src/components/forms/QuotationForm.tsx` returns 0
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>QuotationForm creates/edits quotations, reuses ClientPicker with auto-fill, has a required Valid Until field, and posts to the quotation routes.</done>
</task>

<task type="auto">
  <name>Task 2: New Quotation page + Quotations list page + sidebar nav entry</name>
  <files>src/app/admin/quotations/new/page.tsx, src/app/admin/quotations/page.tsx, src/components/admin/AdminSidebar.tsx</files>
  <read_first>src/app/admin/invoices/new/page.tsx, src/app/admin/invoices/page.tsx (templates), src/lib/quotation-status.ts (STATUS_BADGE/EXPIRED_BADGE/isExpired), src/components/admin/AdminSidebar.tsx (navLinks array)</read_first>
  <action>
    1. Create `src/app/admin/quotations/new/page.tsx` mirroring the new-invoice page: requireAdmin (redirect /admin/login), `const clients = await getClientsForPicker();`, render `<QuotationForm clients={clients} />` inside the same card layout; back link → /admin/quotations; heading "New Quotation"; `export const metadata = { title: "New Quotation" }`.

    2. Create `src/app/admin/quotations/page.tsx` mirroring the invoices list page:
       - `FILTERABLE_STATUSES = ["draft", "sent", "accepted", "declined"] as const;` and FILTER_LINKS (All + those four).
       - Query: `db.select().from(quotations)` with optional `.where(eq(quotations.status, statusFilter))`, `.orderBy(desc(quotations.createdAt))`.
       - Import `formatQuotationNumber` from "@/lib/quotations" and `STATUS_BADGE, EXPIRED_BADGE, isExpired` from "@/lib/quotation-status".
       - Table columns: Reference (`formatQuotationNumber(q.id)`), Client Name, Valid Until (`q.validUntil`), Total (`R{q.totalRands}`), Status (badge + Expired badge when `isExpired(q.status, q.validUntil)`), Actions.
       - Actions cell: a "View" link → `/admin/quotations/${q.id}` AND a "PDF" anchor → `/api/admin/quotations/${q.id}/pdf`.
       - Header actions: a "New Quotation" `.btn-metallic` link → /admin/quotations/new. (No CSV export — invoices had one; quotations don't need it.)
       - `export const metadata = { title: "Quotations" }`, `export const dynamic = "force-dynamic"`.

    3. In `src/components/admin/AdminSidebar.tsx`, add `{ href: "/admin/quotations", label: "Quotations" }` to the `navLinks` array immediately AFTER the Invoices entry (before Automations).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'QuotationForm' src/app/admin/quotations/new/page.tsx` matches
    - `grep -n 'FILTERABLE_STATUSES = \["draft", "sent", "accepted", "declined"\]' src/app/admin/quotations/page.tsx` matches
    - `grep -n 'formatQuotationNumber(q.id)' src/app/admin/quotations/page.tsx` matches
    - `grep -n '/api/admin/quotations/\${q.id}/pdf' src/app/admin/quotations/page.tsx` matches
    - `grep -n 'isExpired(q.status, q.validUntil)' src/app/admin/quotations/page.tsx` matches
    - `grep -n '{ href: "/admin/quotations", label: "Quotations" }' src/components/admin/AdminSidebar.tsx` matches
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>New Quotation page renders the form with picker clients; list page lists/filters quotations with status + Expired badges and a per-row PDF download link; sidebar shows a Quotations entry.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — exits 0
2. `grep 'validUntil' src/components/forms/QuotationForm.tsx`
3. `grep 'formatQuotationNumber' src/app/admin/quotations/page.tsx`
4. `grep '/admin/quotations' src/components/admin/AdminSidebar.tsx`
5. Manual (netlify dev + DEV_AUTH_BYPASS=1): create a quotation via /admin/quotations/new, confirm it appears in the list, filter by status, download its PDF.
</verification>

<success_criteria>
- QuotationForm creates/edits quotations via the picker (auto-fill) with a required Valid Until date
- New Quotation page renders the form
- Quotations list filters by status, shows badges (+ Expired), and links to each PDF
- Sidebar has a Quotations nav entry
</success_criteria>

<output>
After completion, create `.planning/phases/10-quotations/10-05-SUMMARY.md`
</output>
