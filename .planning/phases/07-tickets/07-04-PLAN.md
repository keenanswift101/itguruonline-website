---
phase: 07-tickets
plan: 04
type: execute
wave: 2
depends_on: [07-02]
files_modified:
  - src/components/forms/TicketForm.tsx
  - src/app/admin/tickets/new/page.tsx
  - src/app/admin/tickets/page.tsx
  - src/components/admin/AdminSidebar.tsx
autonomous: true
requirements: [TICKET-01, TICKET-04]

must_haves:
  truths:
    - "TicketForm renders a required ClientPicker + subject input + description textarea + priority select (scheme-dark), and POSTs to /api/admin/tickets"
    - "TicketForm blocks submit with a validation error when no client is selected (client is required — no one-off ticket)"
    - "On 201 the form redirects to /admin/tickets/[id]"
    - "/admin/tickets/new renders TicketForm fed by getClientsForPicker()"
    - "/admin/tickets lists all tickets (subject, client, priority badge, status badge) with All/Open/In Progress/Resolved filter links, open surfaced first"
    - "AdminSidebar has a Tickets nav entry between Clients and Pricing"
  artifacts:
    - path: src/components/forms/TicketForm.tsx
      provides: "Create-ticket form (required client picker + subject/description/priority)"
    - path: src/app/admin/tickets/new/page.tsx
      provides: "New-ticket page feeding clients to TicketForm"
    - path: src/app/admin/tickets/page.tsx
      provides: "Tickets list + status filter (open-first)"
    - path: src/components/admin/AdminSidebar.tsx
      provides: "Tickets nav link"
  key_links:
    - from: src/components/forms/TicketForm.tsx
      to: src/app/api/admin/tickets/route.ts
      via: "fetch POST /api/admin/tickets with { clientId, subject, description, priority }"
      pattern: "/api/admin/tickets"
    - from: src/app/admin/tickets/page.tsx
      to: src/lib/ticket-query.ts
      via: "getTickets(statusFilter) for the list rows"
      pattern: "getTickets"
    - from: src/app/admin/tickets/new/page.tsx
      to: src/lib/client-query.ts
      via: "getClientsForPicker() feeds the required ClientPicker"
      pattern: "getClientsForPicker"
---

<objective>
Build the ticket creation + list UI: `TicketForm` (required `ClientPicker` + subject/description/priority, POSTs to the 07-02 create route), the `/admin/tickets/new` page, the `/admin/tickets` list with a status filter (open-first, via 07-02's `getTickets`), and the "Tickets" sidebar nav entry.

Purpose: This delivers TICKET-01 (create linked to client) and TICKET-04 (list + filter) end-to-end. Depends on 07-02 (create route + getTickets). Parallel with 07-05 (detail UI) — no shared files.

Output: TicketForm + new page + list page + sidebar nav.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-tickets/07-RESEARCH.md
@src/components/forms/ClientPicker.tsx
@src/components/forms/ClientForm.tsx
@src/app/admin/quotations/page.tsx
@src/app/admin/quotations/new/page.tsx
@src/components/admin/AdminSidebar.tsx
@src/lib/ticket-status.ts
@CLAUDE.md
</context>

<interfaces>
From src/components/forms/ClientPicker.tsx: `ClientPicker({ clients: ClientPickerOption[], selectedClientId: number | null, onSelect: (c: ClientPickerOption | null) => void })`. NOTE: it renders a built-in "One-off / no stored client" option that calls onSelect(null); for tickets client is REQUIRED, so TicketForm keeps selectedClientId in state and blocks submit if null (do NOT modify ClientPicker).
From src/lib/client-query.ts: `getClientsForPicker(): Promise<ClientPickerOption[]>` (already exists from Phase 8).
From src/lib/ticket-query.ts (07-02): `getTickets(statusFilter?: TicketStatus): Promise<TicketListItem[]>`.
From src/lib/ticket-status.ts (07-01): `STATUS_BADGE`, `PRIORITY_BADGE` (Record<string,{label,className}>).
Form conventions (from ClientForm.tsx): `useToast`, `Spinner`, `useRouter`, `.btn-metallic`, canonical `text-(--token)`, error state pattern. Native select MUST include `scheme-dark` + `<option className="bg-(--bg-primary) text-(--text-primary)">` (Pitfall 5 / CLAUDE.md).
AdminSidebar navLinks array: insert `{ href: "/admin/tickets", label: "Tickets" }` between Clients and Pricing.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: TicketForm.tsx (required client picker + subject/description/priority)</name>
  <files>src/components/forms/TicketForm.tsx</files>
  <read_first>src/components/forms/ClientForm.tsx (state/toast/error/submit pattern to mirror), src/components/forms/ClientPicker.tsx (props + required-selection handling), src/components/forms/QuotationForm.tsx (how it wires ClientPicker + onSelect into form state)</read_first>
  <action>
    Create src/components/forms/TicketForm.tsx ("use client"):
    - Props: `{ clients: ClientPickerOption[] }`.
    - State: `selectedClientId: number | null` (start null), `subject`, `description`, `priority: "low"|"medium"|"high"` (default "medium"), `errors`, `status: "idle"|"submitting"|"error"`, `serverMessage`.
    - Render (mirror ClientForm's field markup + spacing, `space-y-6`):
      1. A "Client *" field wrapping `<ClientPicker clients={clients} selectedClientId={selectedClientId} onSelect={(c) => setSelectedClientId(c ? c.id : null)} />`. Show a red error under it when the submit-time validation flags a missing client.
      2. A "Subject *" text input (name subject, max 200), same input styling as ClientForm.
      3. A "Description" textarea (name description, rows 4), optional.
      4. A "Priority" native `<select>` with options low/medium/high, default "medium". MUST include `scheme-dark` on the select and `className="bg-(--bg-primary) text-(--text-primary)"` on each `<option>`.
      5. Submit button `.btn-metallic`, "Create Ticket" / Spinner "Creating…" while submitting.
    - validate(): require selectedClientId != null (error "Select a client.") and subject.trim() non-empty (error "Subject is required.").
    - handleSubmit: POST to `/api/admin/tickets` with `JSON.stringify({ clientId: selectedClientId, subject, description, priority })`. On `res.status === 201`, `router.push('/admin/tickets/' + data.id)`. On 422 map `data.fields` to field errors. On other errors show serverMessage + toast.error. Mirror ClientForm's try/catch + toast usage exactly.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '"use client"' src/components/forms/TicketForm.tsx` matches
    - `grep -n '<ClientPicker' src/components/forms/TicketForm.tsx` matches
    - `grep -n '/api/admin/tickets' src/components/forms/TicketForm.tsx` matches
    - `grep -n 'scheme-dark' src/components/forms/TicketForm.tsx` matches
    - `grep -n 'Select a client' src/components/forms/TicketForm.tsx` matches
    - `grep -n "router.push" src/components/forms/TicketForm.tsx` matches
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>TicketForm renders a required ClientPicker + subject/description/priority (scheme-dark select), blocks submit without a client, POSTs to /api/admin/tickets, redirects to the new ticket on 201; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: /admin/tickets list + /admin/tickets/new page + sidebar nav</name>
  <files>src/app/admin/tickets/page.tsx, src/app/admin/tickets/new/page.tsx, src/components/admin/AdminSidebar.tsx</files>
  <read_first>src/app/admin/quotations/page.tsx (list + FILTER_LINKS + Badge pattern to mirror), src/app/admin/quotations/new/page.tsx (new-page shell), src/components/admin/AdminSidebar.tsx (navLinks array), src/lib/ticket-status.ts (STATUS_BADGE, PRIORITY_BADGE)</read_first>
  <action>
    A. Create src/app/admin/tickets/new/page.tsx (mirror quotations/new/page.tsx):
       - `requireAdmin()` → redirect if null; `const clients = await getClientsForPicker();`
       - Back link to /admin/tickets; card wrapper with `<h1>New Ticket</h1>` and `<TicketForm clients={clients} />`.
       - `export const dynamic = "force-dynamic";` + `export const metadata = { title: "New Ticket" };`

    B. Create src/app/admin/tickets/page.tsx (mirror quotations/page.tsx, but rows come from getTickets and there is NO PDF link):
       - `requireAdmin()` → redirect if null.
       - `const { status } = await searchParams;` parse against `["open","in_progress","resolved"]`; `const rows = await getTickets(statusFilter);` (getTickets already applies open-first ordering when no filter).
       - FILTER_LINKS: `[{label:"All",value:undefined},{label:"Open",value:"open"},{label:"In Progress",value:"in_progress"},{label:"Resolved",value:"resolved"}]`, hrefs `/admin/tickets?status=<value>` (or `/admin/tickets` for All), active styling identical to quotations.
       - Header: `<h1>Tickets</h1>` + a "New Ticket" `.btn-metallic` link to /admin/tickets/new.
       - Table columns: Subject | Client | Priority | Status | Actions. Each row: subject links to `/admin/tickets/${t.id}`; client shows `t.clientName`; Priority renders a `PRIORITY_BADGE[t.priority]` badge; Status renders a `STATUS_BADGE[t.status]` badge; Actions has a "View" link to `/admin/tickets/${t.id}`. Reuse the same `Badge` helper + table classes as quotations. Empty state: "No tickets yet."
       - `export const dynamic = "force-dynamic";` + `export const metadata = { title: "Tickets" };`

    C. Edit src/components/admin/AdminSidebar.tsx: insert `{ href: "/admin/tickets", label: "Tickets" },` into the `navLinks` array between the Clients and Pricing entries. Change nothing else.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'getTickets(statusFilter)' src/app/admin/tickets/page.tsx` matches
    - `grep -n 'STATUS_BADGE' src/app/admin/tickets/page.tsx` matches
    - `grep -n 'PRIORITY_BADGE' src/app/admin/tickets/page.tsx` matches
    - `grep -n 'in_progress' src/app/admin/tickets/page.tsx` matches
    - `grep -n '<TicketForm' src/app/admin/tickets/new/page.tsx` matches
    - `grep -n 'getClientsForPicker' src/app/admin/tickets/new/page.tsx` matches
    - `grep -n 'href: "/admin/tickets", label: "Tickets"' src/components/admin/AdminSidebar.tsx` matches
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>/admin/tickets lists tickets with priority/status badges + status filter (open-first), /admin/tickets/new renders TicketForm with clients, sidebar has a Tickets link; tsc clean.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — exits 0
2. `npx vitest run` — full suite green
3. `grep 'getTickets' src/app/admin/tickets/page.tsx`
4. `grep 'label: "Tickets"' src/components/admin/AdminSidebar.tsx`
5. Manual (netlify dev): open /admin/tickets/new, pick a client, set subject + priority, save → redirects to the new ticket; /admin/tickets shows it, open tickets appear before resolved, filter links work.
</verification>

<success_criteria>
- TicketForm creates a client-linked ticket (client required) and redirects to it (TICKET-01)
- /admin/tickets lists all tickets with a status filter, open surfaced first (TICKET-04)
- Sidebar has a Tickets nav entry; full vitest + tsc green
</success_criteria>

<output>
After completion, create `.planning/phases/07-tickets/07-04-SUMMARY.md`
</output>
