---
phase: 04-invoicing
plan: 04
type: execute
wave: 3
depends_on: [01, 02, 03]
files_modified:
  - src/app/admin/invoices/page.tsx
  - src/app/admin/invoices/new/page.tsx
  - src/components/forms/InvoiceForm.tsx
  - src/lib/invoice-status.ts
autonomous: false
requirements: [INVOICE-01, INVOICE-04, INVOICE-07]

must_haves:
  truths:
    - "Owner sees a list of all invoices at /admin/invoices with Invoice #, Client, Due Date, Total, Status, and a red Overdue badge computed at read time (human-checkpoint-verified)"
    - "Owner can filter the list by status, and the Export CSV button carries the active filter through to /api/admin/invoices/csv (human-checkpoint-verified)"
    - "Owner can open /admin/invoices/new, fill client info + line items (auto-summing total), and POST to create a Draft (human-checkpoint-verified)"
    - "Both pages call requireAdmin() first and redirect unauthenticated visitors to /admin/login"
  artifacts:
    - path: "src/app/admin/invoices/page.tsx"
      provides: "invoice list server page with status filter + overdue badge + CSV export button"
      contains: "requireAdmin"
    - path: "src/app/admin/invoices/new/page.tsx"
      provides: "create page wrapping InvoiceForm"
      contains: "requireAdmin"
    - path: "src/components/forms/InvoiceForm.tsx"
      provides: "client-side invoice create form with line item editor"
      contains: "use client"
    - path: "src/lib/invoice-status.ts"
      provides: "isOverdue + status badge helpers (shared with detail page)"
      contains: "export function isOverdue"
  key_links:
    - from: "src/components/forms/InvoiceForm.tsx"
      to: "/api/admin/invoices"
      via: "fetch POST on submit"
      pattern: "fetch\\(.*api/admin/invoices"
    - from: "src/app/admin/invoices/page.tsx"
      to: "/api/admin/invoices/csv"
      via: "Export CSV link with status query param"
      pattern: "invoices/csv"
---

<objective>
Build the invoice list page (status filter, overdue badge, CSV export) and the create form page. This is the owner's entry point to invoicing: see everything, filter, export, and create new Drafts.

Purpose: INVOICE-01 (create UI), INVOICE-04 (status tracking + computed Overdue on the list), INVOICE-07 (CSV export button).
Output: /admin/invoices list, /admin/invoices/new create page, InvoiceForm client component, shared invoice-status helpers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-invoicing/04-CONTEXT.md
@CLAUDE.md

<interfaces>
From src/lib/db/schema.ts: invoices, invoiceLineItems.
From src/lib/invoices.ts (04-02): `formatInvoiceNumber(fiscalYear, sequenceNumber)`, `invoiceInput` (line item shape).
From src/lib/auth.ts: `requireAdmin()`.
POST /api/admin/invoices (04-02) body shape:
```typescript
{ clientName: string; clientEmail?: string; billingAddress?: string;
  issueDate: "YYYY-MM-DD"; dueDate: "YYYY-MM-DD";
  lineItems: { description: string; quantity: number; unitPriceRands: number }[] }
```
Returns 201 `{ ok: true, id }`.
GET /api/admin/invoices/csv (04-03) accepts `?status=draft|sent|paid`.
Existing admin page pattern (src/app/admin/dashboard/page.tsx): `async function Page()`, `await requireAdmin(); if (!session) redirect("/admin/login")`, glass panel `bg-(--bg-primary)/80 backdrop-blur-sm rounded-xl border border-(--border-color)`.
</interfaces>

<design_constraints>
- Server components by default; "use client" ONLY on InvoiceForm (needs state for the line item editor + auto-sum).
- Tailwind v4 arbitrary-value syntax: `text-(--text-secondary)`, `bg-(--bg-primary)` — NOT `text-[var(--text-secondary)]`.
- Buttons: `.btn-metallic` for the primary "New Invoice" / "Create" CTA, `.btn-glass` for secondary "Export CSV".
- NO per-section bg fills / no `bg-slate-900` — the admin layout already provides the fixed bg-image.jpg; content panels use the glass panel style over transparent.
- Status badge colors (D-06 discretion): draft=gray, sent=cobalt `#00aaff`, paid=green, overdue=red. Overdue is a SECOND badge alongside "Sent", not a replacement.
- Money display on screen: `R{price}` integer is acceptable for the list (matches Services page); the formal `R x.toFixed(2)` lives in the PDF.
- Do NOT reference an AdminSidebar component — it may not exist on this branch. Provide a simple back-link to /admin/dashboard and a page heading instead. (If a sidebar exists, activating its Invoices link is optional and out of this plan's required scope.)
</design_constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared invoice-status helpers + invoice list page</name>
  <read_first>
    - src/app/admin/dashboard/page.tsx (auth-first + glass panel pattern)
    - .planning/phases/04-invoicing/04-CONTEXT.md D-06 (overdue = status 'sent' && dueDate < today), specifics (two badges)
    - src/lib/invoices.ts (formatInvoiceNumber)
  </read_first>
  <action>
    1. Create `src/lib/invoice-status.ts`:
       ```typescript
       export function isOverdue(status: string, dueDate: string): boolean {
         return status === "sent" && new Date(dueDate) < new Date(new Date().toISOString().slice(0, 10));
       }
       export const STATUS_BADGE: Record<string, { label: string; className: string }> = {
         draft: { label: "Draft", className: "bg-(--bg-primary) text-(--text-secondary) border border-(--border-color)" },
         sent:  { label: "Sent",  className: "text-[#00aaff] border border-[#00aaff]/40" },
         paid:  { label: "Paid",  className: "text-green-400 border border-green-500/40" },
       };
       export const OVERDUE_BADGE = { label: "Overdue", className: "text-red-400 border border-red-500/50" };
       ```
    2. Create `src/app/admin/invoices/page.tsx` — `export const dynamic = "force-dynamic"`, `export const metadata = { title: "Invoices" }`, `async function Page({ searchParams }: { searchParams: Promise<{ status?: string }> })`:
       - `const session = await requireAdmin(); if (!session) redirect("/admin/login");` FIRST.
       - `const { status } = await searchParams;` — if draft|sent|paid, filter the select with `eq(invoices.status, status)`, else all. Order by createdAt desc.
       - Render inside a glass panel: heading "Invoices", a "New Invoice" `.btn-metallic` link to `/admin/invoices/new`, an "Export CSV" `.btn-glass` link to `/api/admin/invoices/csv${status ? "?status="+status : ""}`, and status filter links (All / Draft / Sent / Paid) that set the `?status=` query.
       - Table columns: Invoice #, Client Name, Due Date, Total, Status, Actions. Invoice # via `formatInvoiceNumber`. Status cell renders the STATUS_BADGE for the stored status, plus the OVERDUE_BADGE when `isOverdue(inv.status, inv.dueDate)`. Actions: a "View" link to `/admin/invoices/${inv.id}`.
       - Empty state: "No invoices yet." when the list is empty.
       - Back link to /admin/dashboard.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "requireAdmin()" src/app/admin/invoices/page.tsx` first statement in Page
    - `grep -n "export function isOverdue" src/lib/invoice-status.ts` matches
    - `grep -n "invoices/csv" src/app/admin/invoices/page.tsx` matches (export button carries status)
    - `grep -n "btn-metallic" src/app/admin/invoices/page.tsx` matches (New Invoice CTA)
    - `grep -n "formatInvoiceNumber" src/app/admin/invoices/page.tsx` matches
    - `grep -n "text-\[var(" src/app/admin/invoices/page.tsx` returns NOTHING (Tailwind v4 syntax rule)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>List page shows all invoices with status + computed overdue badge, filters by status, and exports the filtered CSV; auth-guarded; tsc-clean.</done>
</task>

<task type="auto">
  <name>Task 2: InvoiceForm client component + /admin/invoices/new page</name>
  <read_first>
    - src/app/admin/invoices/page.tsx (Task 1 — for consistent styling)
    - .planning/phases/04-invoicing/04-CONTEXT.md D-02 (line item model), D-03 (form fields, one-page)
    - Any existing client form for the fetch+error pattern (e.g. src/components/forms/ContactForm.tsx if present)
  </read_first>
  <action>
    1. Create `src/components/forms/InvoiceForm.tsx` — `"use client"`. State:
       - client fields: clientName, clientEmail, billingAddress, issueDate (default today `new Date().toISOString().slice(0,10)`), dueDate (empty, required).
       - lineItems: array of `{ description, quantity, unitPriceRands }`, start with one empty row. "Add Item" button appends a row; each row has a "Remove" button.
       - Auto-sum: derive each line total (`quantity * unitPriceRands`) and the invoice total live as the owner types (display `R{total}`).
       - Submit: `POST /api/admin/invoices` with the D-03 body shape. On 201 → `router.push("/admin/invoices/" + id)` (use `useRouter` from next/navigation). On 422 → show field errors; on other errors → show a message.
       - Use `.btn-metallic` for "Create Invoice", `.btn-glass` for "Add Item". Tailwind v4 syntax only. Inputs styled consistent with the glass panel theme.
       - Basic client validation: clientName + dueDate required, quantity min 1.
    2. Create `src/app/admin/invoices/new/page.tsx` — `export const dynamic = "force-dynamic"`, `export const metadata = { title: "New Invoice" }`, `async function Page()`: `requireAdmin()` first → redirect; render a glass panel with heading "New Invoice", a back link to /admin/invoices, and `<InvoiceForm />`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '"use client"' src/components/forms/InvoiceForm.tsx` matches (first line)
    - `grep -n "requireAdmin()" src/app/admin/invoices/new/page.tsx` first statement in Page
    - `grep -n "fetch(" src/components/forms/InvoiceForm.tsx` targets `/api/admin/invoices`
    - `grep -n "Add Item" src/components/forms/InvoiceForm.tsx` matches
    - `grep -n "btn-metallic" src/components/forms/InvoiceForm.tsx` matches
    - `grep -n "text-\[var(" src/components/forms/InvoiceForm.tsx` returns NOTHING
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Owner can create a Draft invoice with inline line items that auto-sum; on success redirected to the detail page; auth-guarded; tsc-clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Human verification</name>
  <action>Pause for the owner to manually verify the built UI in a browser per the steps in how-to-verify below. Resume only after they approve.</action>
  <what-built>Invoice list page (status filter, overdue badge, CSV export) and the create form with an auto-summing line item editor.</what-built>
  <how-to-verify>
    1. Run `npm run dev`, log in at /admin/login, visit http://localhost:3000/admin/invoices — confirm the page loads with heading, filter links, New Invoice + Export CSV buttons (empty state if no data).
    2. Click "New Invoice". Fill client name + a due date, add 2 line items with quantities/prices — confirm each line total and the invoice total update live as you type.
    3. Click "Create Invoice" — confirm you're redirected to the new invoice's detail page and it now appears back on the list as "Draft".
    4. On the list, click "Export CSV" — confirm invoices.csv downloads with the correct column header row.
    5. If any Sent invoice exists with a past due date, confirm it shows a red "Overdue" badge alongside "Sent".
  </how-to-verify>
  <resume-signal>Type "approved" or describe what's off (styling, auto-sum, redirect, CSV columns).</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0
- Human checkpoint approves list + create flow + CSV download + overdue badge
</verification>

<success_criteria>
- /admin/invoices lists invoices with status + computed overdue, filterable, CSV export honors filter
- /admin/invoices/new creates a Draft via the form with auto-summing line items
- Auth-guarded, Tailwind v4 syntax, btn-metallic/btn-glass, no per-section bg fills
</success_criteria>

<output>
After completion, create `.planning/phases/04-invoicing/04-04-SUMMARY.md` documenting the InvoiceForm state shape, the overdue helper location (src/lib/invoice-status.ts, reused by 04-05), badge color choices, and any checkpoint feedback applied.
</output>
