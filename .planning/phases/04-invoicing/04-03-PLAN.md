---
phase: 04-invoicing
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - src/app/api/admin/invoices/[id]/status/route.ts
  - src/components/pdf/InvoiceDocument.tsx
  - src/app/api/admin/invoices/[id]/pdf/route.tsx
  - src/app/api/admin/invoices/csv/route.ts
  - src/app/api/admin/invoices/[id]/status/route.test.ts
  - src/app/api/admin/invoices/[id]/pdf/route.test.ts
  - src/app/api/admin/invoices/csv/route.test.ts
autonomous: true
requirements: [INVOICE-03, INVOICE-04, INVOICE-05, INVOICE-06, INVOICE-07]

must_haves:
  truths:
    - "PATCH status transitions Draft→Sent (assigns gapless INV-YYYY-NNN number atomically), Sent→Paid (records paid_at), and the reverse undos; invalid transitions return 409"
    - "Number assignment is gapless: deleted Drafts never consume a number, sequence assigned only at Draft→Sent"
    - "GET /pdf returns a downloadable application/pdf with plain 'Invoice'/'Draft Invoice' labeling — never 'Tax Invoice', never VAT"
    - "GET /csv returns text/csv with the exact column header row and honors status filter query param"
    - "All routes 401 without session"
  artifacts:
    - path: "src/app/api/admin/invoices/[id]/status/route.ts"
      provides: "PATCH status transitions + atomic number assignment"
      exports: ["PATCH"]
    - path: "src/components/pdf/InvoiceDocument.tsx"
      provides: "react-pdf invoice document (no Tailwind, StyleSheet only)"
      contains: "StyleSheet.create"
    - path: "src/app/api/admin/invoices/[id]/pdf/route.tsx"
      provides: "renderToBuffer PDF response"
      exports: ["GET"]
    - path: "src/app/api/admin/invoices/csv/route.ts"
      provides: "CSV export with csvEscape"
      exports: ["GET"]
  key_links:
    - from: "src/app/api/admin/invoices/[id]/status/route.ts"
      to: "gapless sequence_number"
      via: "single atomic UPDATE with correlated subquery"
      pattern: "MAX\\(sequence_number\\)"
    - from: "src/app/api/admin/invoices/[id]/pdf/route.tsx"
      to: "src/components/pdf/InvoiceDocument.tsx"
      via: "renderToBuffer(<InvoiceDocument .../>)"
      pattern: "renderToBuffer"
---

<objective>
Build the read/transition side: PATCH status (the numbering engine + mark-paid), the PDF document + download route, and the CSV export. This is the highest-risk plan — it contains the gapless-numbering atomic UPDATE and the SARS-compliance labeling rules. Convert the three Wave 0 stub test files to real tests.

Purpose: INVOICE-03 (gapless numbering + plain labeling), INVOICE-04 (status tracking), INVOICE-05 (mark paid), INVOICE-06 (single PDF), INVOICE-07 (CSV export).
Output: status PATCH route, InvoiceDocument.tsx, PDF GET route, CSV GET route, real tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-invoicing/04-CONTEXT.md
@.planning/phases/04-invoicing/04-RESEARCH.md
@CLAUDE.md

<interfaces>
From src/lib/db/schema.ts (04-01): invoices, invoiceLineItems (see 04-01/04-02).
From src/lib/invoices.ts (04-02): `formatInvoiceNumber(fiscalYear, sequenceNumber)` returns "DRAFT" or "INV-YYYY-NNN".
From src/lib/auth.ts: `requireAdmin()`.
From src/lib/db/index.ts: `db` (neon-http). Use `db.execute(sql\`...\`)` for the atomic UPDATE (RESEARCH section 2 — NOT FOR UPDATE, NOT WebSocket, NOT ws package).
react-pdf server import (RESEARCH section 1):
```typescript
import { renderToBuffer, Document, Page, Text, View, StyleSheet } from "@react-pdf/renderer";
```
</interfaces>

<security_constraints>
- NEVER the words "Tax Invoice", "VAT", "Total incl. VAT" anywhere in InvoiceDocument.tsx, the PDF, or any response (INVOICE-03 / D-05, SARS compliance).
- PATCH must validate allowed transitions server-side; reject unknown/invalid transitions with 409. Do not trust the client.
- requireAdmin() first in every route.
</security_constraints>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PATCH status route — transitions + atomic gapless numbering + mark paid</name>
  <read_first>
    - .planning/phases/04-invoicing/04-RESEARCH.md section 2 (atomic single-UPDATE numbering — the "Best option" block) and D-06/D-09 in CONTEXT
    - src/lib/db/index.ts (db.execute + sql tag usage)
    - src/app/api/admin/invoices/route.ts (04-02 auth/validation shape to mirror)
  </read_first>
  <behavior>
    - PATCH without session → 401
    - PATCH bad body / invalid status value → 422
    - Draft→sent (DB): sets status='sent', fiscal_year=year of issueDate (or current year), sequence_number = MAX(sequence_number for that fiscal_year)+1; number is gapless
    - sent→paid (DB): status='paid', paid_at = NOW()
    - paid→sent (DB): status='sent', paid_at = NULL
    - sent→draft (DB): status='draft', fiscal_year=NULL, sequence_number=NULL (clears number per D-06 recommendation)
    - Any transition not in the allowed set → 409
  </behavior>
  <action>
    1. Create `src/app/api/admin/invoices/[id]/status/route.ts`, `export const dynamic = "force-dynamic"`.
    2. Zod: `z.object({ status: z.enum(["draft", "sent", "paid"]) })`.
    3. `PATCH(req, { params })`:
       - `requireAdmin()` first → 401.
       - parse id (NaN → 400), parse+validate body (400/422).
       - Load current: `const [inv] = await db.select({ status: invoices.status, issueDate: invoices.issueDate }).from(invoices).where(eq(invoices.id, numId));` → 404 if none.
       - Define allowed transitions map: `{ draft: ["sent"], sent: ["paid", "draft"], paid: ["sent"] }`. If `!allowed[inv.status]?.includes(target)` → 409 `{ error: "Invalid status transition." }`.
       - **draft→sent (atomic gapless number, RESEARCH section 2 "Best option"):**
         ```typescript
         import { sql } from "drizzle-orm";
         const fiscalYear = new Date(inv.issueDate).getFullYear();
         await db.execute(sql`
           UPDATE invoices
           SET status = 'sent',
               fiscal_year = ${fiscalYear},
               sequence_number = (
                 SELECT COALESCE(MAX(sequence_number), 0) + 1
                 FROM invoices
                 WHERE fiscal_year = ${fiscalYear}
               )
           WHERE id = ${numId} AND status = 'draft'
         `);
         ```
         Do NOT use `FOR UPDATE`, do NOT create a WebSocket Pool, do NOT import ws. Document inline: "// single-admin safe at READ COMMITTED; upgrade to SERIALIZABLE for multi-user".
       - **sent→paid:** `db.update(invoices).set({ status: "paid", paidAt: new Date() }).where(eq(invoices.id, numId))`.
       - **paid→sent:** `.set({ status: "sent", paidAt: null })`.
       - **sent→draft:** `.set({ status: "draft", fiscalYear: null, sequenceNumber: null })`.
       - Return `{ ok: true }` 200.
    4. Replace todos in `src/app/api/admin/invoices/[id]/status/route.test.ts`: non-DB 401; describeIfDb tests: create draft → PATCH sent → select and assert fiscal_year != null && sequence_number != null; PATCH paid → assert paid_at IS NOT NULL; assert an invalid transition (e.g. draft→paid) returns 409.
  </action>
  <verify>
    <automated>npx vitest run "src/app/api/admin/invoices/[id]/status/route.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "await requireAdmin()" "src/app/api/admin/invoices/[id]/status/route.ts"` first statement in PATCH
    - `grep -n "MAX(sequence_number)" "src/app/api/admin/invoices/[id]/status/route.ts"` matches
    - `grep -ni "for update" "src/app/api/admin/invoices/[id]/status/route.ts"` returns NOTHING
    - `grep -n "@neondatabase/serverless\|import ws\|new Pool" "src/app/api/admin/invoices/[id]/status/route.ts"` returns NOTHING
    - `grep -n "status: 409" "src/app/api/admin/invoices/[id]/status/route.ts"` matches (invalid transition)
    - `grep -n "paidAt: new Date()" "src/app/api/admin/invoices/[id]/status/route.ts"` matches
    - `grep -cn "it.todo" "src/app/api/admin/invoices/[id]/status/route.test.ts"` is 0
    - test file passes
  </acceptance_criteria>
  <done>Status transitions enforced server-side (409 on invalid); Draft→Sent assigns gapless number via single atomic UPDATE; paid_at recorded; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: InvoiceDocument.tsx (react-pdf) + GET /pdf download route</name>
  <read_first>
    - .planning/phases/04-invoicing/04-RESEARCH.md section 1 (react-pdf StyleSheet + route pattern) and section 4 (Response pattern)
    - src/app/api/admin/invoices/test-pdf/route.tsx (04-01 smoke route — proven working import path)
    - src/lib/invoices.ts (formatInvoiceNumber)
    - .planning/phases/04-invoicing/04-CONTEXT.md D-01, D-05, and Claude's Discretion (PDF visual design + bank footer)
  </read_first>
  <action>
    1. Create `src/components/pdf/InvoiceDocument.tsx` — a pure server component (no "use client", no hooks). Props: `{ invoice: {...}, lineItems: [...] }`.
       - `import { Document, Page, Text, View, StyleSheet } from "@react-pdf/renderer";`
       - Use `StyleSheet.create({...})` ONLY — NO Tailwind, NO className. Built-in fonts Helvetica / Helvetica-Bold only (no custom font registration).
       - Heading: `invoice.status === "draft" ? "Draft Invoice" : "Invoice"`. NEVER "Tax Invoice".
       - Top-right: "IT-Guru Online" name. Top-left client block: clientName, clientEmail, billingAddress.
       - Number line: `formatInvoiceNumber(invoice.fiscalYear, invoice.sequenceNumber)` (shows "DRAFT" for drafts).
       - Issue Date / Due Date rows.
       - Line items table: Description | Qty | Unit Price | Line Total. Money formal: `R ${(cents-free integer).toFixed(2)}` e.g. `R 1500.00`.
       - Total row: `R ${invoice.totalRands.toFixed(2)}`. NO VAT row, NO "incl. VAT".
       - Footer: hardcoded IT-Guru EFT bank details (static text — no DB) + "Thank you for your business."
       - status badge/stamp: "PAID" stamp when status==='paid' (Claude's discretion).
    2. Create `src/app/api/admin/invoices/[id]/pdf/route.tsx`, `export const dynamic = "force-dynamic"`, `GET(_req, { params })`:
       - `requireAdmin()` first → `new Response("Unauthorized", { status: 401 })`.
       - parse id; load invoice + its line items (`db.select ... where eq(invoiceLineItems.invoiceId, numId)` ordered by sortOrder). 404 if invoice missing.
       - `const buffer = await renderToBuffer(<InvoiceDocument invoice={invoice} lineItems={lineItems} />);`
       - filename: draft → `"Draft-Invoice.pdf"`, else `\`Invoice-${formatInvoiceNumber(...)}.pdf\``.
       - `return new Response(buffer, { headers: { "Content-Type": "application/pdf", "Content-Disposition": \`attachment; filename="${filename}"\`, "Cache-Control": "private, no-store" } });`
       - This file uses JSX (renderToBuffer(<InvoiceDocument .../>)), so it MUST be named `route.tsx` (already reflected in files_modified).
    3. Replace todos in `src/app/api/admin/invoices/[id]/pdf/route.test.ts`: non-DB 401; describeIfDb test that creates an invoice, calls GET, asserts 200 + content-type application/pdf + body length > 0.
  </action>
  <verify>
    <automated>npx vitest run "src/app/api/admin/invoices/[id]/pdf/route.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "StyleSheet.create" src/components/pdf/InvoiceDocument.tsx` matches
    - `grep -rni "tax invoice\|VAT\|incl. vat" src/components/pdf/InvoiceDocument.tsx` returns NOTHING (SARS rule)
    - `grep -n "className" src/components/pdf/InvoiceDocument.tsx` returns NOTHING (no Tailwind in react-pdf)
    - `grep -n "Draft Invoice" src/components/pdf/InvoiceDocument.tsx` matches
    - `grep -n "renderToBuffer" "src/app/api/admin/invoices/[id]/pdf/route.tsx"` matches
    - `grep -n "await requireAdmin()" "src/app/api/admin/invoices/[id]/pdf/route.tsx"` first statement in GET
    - pdf test file passes
  </acceptance_criteria>
  <done>Downloadable PDF renders with plain Invoice/Draft Invoice labeling, no VAT, bank footer; 401 guarded; test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GET /csv export route (filterable)</name>
  <read_first>
    - .planning/phases/04-invoicing/04-RESEARCH.md section 5 (csvEscape helper + response headers) and D-08 (CSV columns) in CONTEXT
    - src/lib/invoices.ts (formatInvoiceNumber for the Invoice # column)
  </read_first>
  <behavior>
    - GET without session → 401
    - GET → Content-Type text/csv; charset=utf-8, Content-Disposition attachment filename="invoices.csv"
    - First row exactly: `Invoice #,Client Name,Client Email,Issue Date,Due Date,Total (R),Status,Paid At`
    - `?status=sent` filters rows to that status
  </behavior>
  <action>
    1. Create `src/app/api/admin/invoices/csv/route.ts`, `export const dynamic = "force-dynamic"`, `GET(req)`:
       - `requireAdmin()` first → 401.
       - Read `status` from `new URL(req.url).searchParams`; if one of draft|sent|paid, filter the query with `eq(invoices.status, status)`; otherwise select all. Order by createdAt desc.
       - Define a local `csvEscape(val)` helper (copy from RESEARCH section 5): wraps in quotes + doubles inner quotes when the value contains `,` `"` or newline.
       - Header row: `Invoice #,Client Name,Client Email,Issue Date,Due Date,Total (R),Status,Paid At`.
       - Each row: `[formatInvoiceNumber(inv.fiscalYear, inv.sequenceNumber), csvEscape(inv.clientName), csvEscape(inv.clientEmail ?? ""), inv.issueDate, inv.dueDate, inv.totalRands, inv.status, inv.paidAt ? new Date(inv.paidAt).toISOString() : ""].join(",")`.
       - `return new Response(csv, { headers: { "Content-Type": "text/csv; charset=utf-8", "Content-Disposition": 'attachment; filename="invoices.csv"' } });`
    2. Replace todos in `src/app/api/admin/invoices/csv/route.test.ts`: non-DB 401; a test that (mock or describeIfDb) asserts Content-Type contains text/csv and the body's first line equals the exact header spec.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/admin/invoices/csv/route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "await requireAdmin()" src/app/api/admin/invoices/csv/route.ts` first statement in GET
    - `grep -n "text/csv" src/app/api/admin/invoices/csv/route.ts` matches
    - `grep -n "Invoice #,Client Name,Client Email,Issue Date,Due Date,Total (R),Status,Paid At" src/app/api/admin/invoices/csv/route.ts` matches exactly
    - `grep -n "csvEscape" src/app/api/admin/invoices/csv/route.ts` matches
    - `grep -cn "it.todo" src/app/api/admin/invoices/csv/route.test.ts` is 0
    - csv test file passes
  </acceptance_criteria>
  <done>CSV export returns correct headers + column row, honors status filter, 401 guarded; test green.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/app/api/admin/invoices` all green (0 todos remaining across the phase's route tests)
- `npx tsc --noEmit` exits 0
- `grep -rni "tax invoice\|incl. vat" src/components/pdf src/app/api/admin/invoices` returns NOTHING (SARS gate)
- No `ws` / WebSocket Pool / FOR UPDATE anywhere in the status route
</verification>

<success_criteria>
- Status transitions enforced (409 invalid); gapless numbering via atomic UPDATE; mark-paid records paid_at
- PDF downloads with plain labeling, no VAT, bank footer
- CSV export correct + filterable
</success_criteria>

<output>
After completion, create `.planning/phases/04-invoicing/04-03-SUMMARY.md` documenting: the exact allowed-transition map, confirmation the pdf route is route.tsx, the final CSV column order, the InvoiceDocument prop shape (so 04-05 can link "Download PDF" correctly), and confirmation no VAT/Tax-Invoice strings exist.
</output>
