---
phase: 04-invoicing
plan: 05
type: execute
wave: 2
depends_on: [02, 03, 04]
files_modified:
  - src/app/admin/invoices/[id]/page.tsx
  - src/components/forms/InvoiceStatusActions.tsx
autonomous: false
requirements: [INVOICE-02, INVOICE-04, INVOICE-05, INVOICE-06]

must_haves:
  truths:
    - "Owner opens /admin/invoices/[id] and sees full invoice details, current status, computed Overdue badge, and line items"
    - "A Draft invoice renders the editable InvoiceForm (prefilled) and saving PUTs to update; Sent/Paid invoices render read-only (edit form NOT rendered)"
    - "Status action buttons drive the lifecycle: Mark Sent (draft), Mark Paid (sent/overdue), Unpublish (sent→draft), Undo Paid (paid→sent) — each PATCHes then refreshes"
    - "A Download PDF button links to /api/admin/invoices/[id]/pdf; a Delete button appears only on Drafts"
    - "Page calls requireAdmin() first and redirects unauthenticated visitors"
  artifacts:
    - path: "src/app/admin/invoices/[id]/page.tsx"
      provides: "invoice detail page (edit for draft, read-only otherwise)"
      contains: "requireAdmin"
    - path: "src/components/forms/InvoiceStatusActions.tsx"
      provides: "client status-transition + delete buttons"
      contains: "use client"
  key_links:
    - from: "src/components/forms/InvoiceStatusActions.tsx"
      to: "/api/admin/invoices/[id]/status"
      via: "fetch PATCH { status }"
      pattern: "invoices/.*status"
    - from: "src/app/admin/invoices/[id]/page.tsx"
      to: "/api/admin/invoices/[id]/pdf"
      via: "Download PDF link"
      pattern: "invoices/.*pdf"
    - from: "src/app/admin/invoices/[id]/page.tsx"
      to: "InvoiceForm (edit mode)"
      via: "conditional render when status === draft"
      pattern: "status === \"draft\""
---

<objective>
Build the invoice detail page — the hub for a single invoice's lifecycle: view details, edit (Drafts only), transition status, download PDF, and delete (Drafts only). This closes the loop opened by the list/create pages in 04-04.

Purpose: INVOICE-02 (edit Draft only — enforced by conditional edit-form render on top of the server-side 409 lock from 04-02), INVOICE-04 (status transitions + computed Overdue), INVOICE-05 (Mark Paid), INVOICE-06 (Download PDF).
Output: /admin/invoices/[id] detail page, InvoiceStatusActions client component.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-invoicing/04-CONTEXT.md
@CLAUDE.md

<interfaces>
From src/lib/db/schema.ts: invoices, invoiceLineItems.
From src/lib/invoices.ts (04-02): `formatInvoiceNumber`.
From src/lib/invoice-status.ts (04-04): `isOverdue(status, dueDate)`, `STATUS_BADGE`, `OVERDUE_BADGE`.
From src/components/forms/InvoiceForm.tsx (04-04): the create form. This plan must make it reusable for EDIT — it needs an optional `initial` prop (existing invoice + line items) and an optional `invoiceId` prop; when present it PUTs to `/api/admin/invoices/[id]` instead of POSTing. If 04-04 built it POST-only, extend it here (add props, branch the fetch method/URL) — files_modified may include InvoiceForm.tsx if so; note it in the SUMMARY.
API contracts:
- PUT /api/admin/invoices/[id] (04-02): same body as POST; returns 409 if not draft.
- PATCH /api/admin/invoices/[id]/status (04-03): body `{ status: "draft"|"sent"|"paid" }`; 409 on invalid transition.
- DELETE /api/admin/invoices/[id] (04-02): 200 on draft, 409 otherwise.
- GET /api/admin/invoices/[id]/pdf (04-03): binary download.
From src/app/admin/dashboard/page.tsx: auth-first + glass panel pattern.
</interfaces>

<design_constraints>
- Server component page; "use client" only on InvoiceStatusActions.
- Tailwind v4 syntax (`text-(--text-secondary)`), btn-metallic (primary action) / btn-glass (secondary), no per-section bg fills.
- Status buttons visibility (D-06/D-09): Mark Sent only when status==='draft'; Mark Paid only when status==='sent' (includes overdue, since overdue is computed from sent); Unpublish only when status==='sent'; Undo Paid only when status==='paid'; Delete only when status==='draft'.
- Money on screen `R{price}`. Overdue badge red, second badge alongside "Sent".
- The edit form must NOT be rendered when status !== 'draft' (defense-in-depth with the server 409).
</design_constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: InvoiceStatusActions client component</name>
  <read_first>
    - .planning/phases/04-invoicing/04-CONTEXT.md D-06 (transitions), D-09 (mark paid), specifics (delete draft)
    - src/components/forms/InvoiceForm.tsx (04-04 — fetch + error + router.refresh pattern to mirror)
    - src/app/api/admin/invoices/[id]/status/route.ts (04-03 — allowed transitions)
  </read_first>
  <action>
    1. Create `src/components/forms/InvoiceStatusActions.tsx` — `"use client"`. Props: `{ id: number; status: string }`.
       - `useRouter` from next/navigation.
       - `async function patch(target: string)`: `await fetch("/api/admin/invoices/" + id + "/status", { method: "PATCH", headers: {"Content-Type":"application/json"}, body: JSON.stringify({ status: target }) })`; on ok → `router.refresh()`; on 409 → show inline error "Invalid transition."; on other → generic error. Guard against double-submit with a pending state.
       - `async function del()`: confirm dialog → `fetch("/api/admin/invoices/" + id, { method: "DELETE" })`; on ok → `router.push("/admin/invoices")`.
       - Render buttons conditionally on `status`:
         - status==='draft': "Mark Sent" (.btn-metallic → patch("sent")) + "Delete" (.btn-glass, red text → del()).
         - status==='sent': "Mark Paid" (.btn-metallic → patch("paid")) + "Unpublish" (.btn-glass → patch("draft")).
         - status==='paid': "Undo Paid" (.btn-glass → patch("sent")).
       - Show the inline error message area when set.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '"use client"' src/components/forms/InvoiceStatusActions.tsx` first line
    - `grep -n "status/route\|/status" src/components/forms/InvoiceStatusActions.tsx` matches (PATCH target)
    - `grep -n "router.refresh()" src/components/forms/InvoiceStatusActions.tsx` matches
    - `grep -n "Mark Sent" src/components/forms/InvoiceStatusActions.tsx` and `Mark Paid` both match
    - `grep -n "method: \"DELETE\"" src/components/forms/InvoiceStatusActions.tsx` matches
    - `grep -n "text-\[var(" src/components/forms/InvoiceStatusActions.tsx` returns NOTHING
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Status buttons render per current status, PATCH transitions + refresh, Draft delete works; tsc-clean.</done>
</task>

<task type="auto">
  <name>Task 2: Invoice detail page (edit for Draft, read-only otherwise, PDF + actions)</name>
  <read_first>
    - src/app/admin/dashboard/page.tsx (auth-first + glass panel)
    - src/components/forms/InvoiceForm.tsx (04-04 — extend for edit prop if needed)
    - src/lib/invoice-status.ts + src/lib/invoices.ts (helpers)
  </read_first>
  <action>
    1. If needed, extend `src/components/forms/InvoiceForm.tsx` to accept optional `initial?: { clientName; clientEmail; billingAddress; issueDate; dueDate; lineItems: {...}[] }` and `invoiceId?: number`. When `invoiceId` is set: prefill state from `initial`, and on submit `PUT /api/admin/invoices/${invoiceId}` (else POST as before). On PUT success → `router.refresh()` (stay on detail) or `router.push` back to detail. Handle a 409 response by showing "This invoice can no longer be edited." (edge case if status changed in another tab).
    2. Create `src/app/admin/invoices/[id]/page.tsx` — `export const dynamic = "force-dynamic"`, `async function Page({ params }: { params: Promise<{ id: string }> })`:
       - `const session = await requireAdmin(); if (!session) redirect("/admin/login");` FIRST.
       - `const { id } = await params;` parse number; load invoice; if none → `notFound()` (next/navigation).
       - Load line items ordered by sortOrder.
       - Header: invoice number via `formatInvoiceNumber` (or "DRAFT"), STATUS_BADGE + OVERDUE_BADGE when `isOverdue`. Back link to /admin/invoices.
       - Action bar: `<InvoiceStatusActions id={inv.id} status={inv.status} />` + a "Download PDF" link (`.btn-glass`) to `/api/admin/invoices/${inv.id}/pdf`.
       - Body:
         - **If `inv.status === "draft"`**: render `<InvoiceForm invoiceId={inv.id} initial={{ ...client fields, lineItems }} />` (editable, prefilled).
         - **Else (sent/paid)**: render a READ-ONLY details block — client info, dates, a line items table, and total `R{inv.totalRands}`. Do NOT render InvoiceForm.
       - Show paid_at when status==='paid'.
       - All in glass panels; Tailwind v4 syntax; no per-section bg fills.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "requireAdmin()" "src/app/admin/invoices/[id]/page.tsx"` first statement in Page
    - `grep -n 'status === "draft"' "src/app/admin/invoices/[id]/page.tsx"` matches (conditional edit render)
    - `grep -n "InvoiceStatusActions" "src/app/admin/invoices/[id]/page.tsx"` matches
    - `grep -n "invoices/.*pdf\|/pdf" "src/app/admin/invoices/[id]/page.tsx"` matches (Download PDF link)
    - `grep -n "isOverdue\|OVERDUE_BADGE" "src/app/admin/invoices/[id]/page.tsx"` matches
    - `grep -n "notFound\|redirect" "src/app/admin/invoices/[id]/page.tsx"` matches
    - `grep -n "text-\[var(" "src/app/admin/invoices/[id]/page.tsx"` returns NOTHING
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Detail page shows full invoice; Draft is editable via prefilled form, Sent/Paid is read-only; PDF + status actions wired; auth-guarded; tsc-clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Human verification</name>
  <action>Pause for the owner to manually verify the built UI in a browser per the steps in how-to-verify below. Resume only after they approve.</action>
  <what-built>Invoice detail page with edit-for-Draft / read-only-otherwise rendering, status lifecycle buttons, Download PDF, and Draft delete.</what-built>
  <how-to-verify>
    1. `npm run dev`, log in, open a Draft invoice from /admin/invoices. Confirm the editable form appears prefilled. Change a line item, save — confirm the total updates.
    2. Click "Mark Sent" — confirm the page refreshes, the invoice now shows an INV-YYYY-NNN number, status "Sent", and the form is replaced by a read-only view (no editing possible).
    3. Click "Download PDF" — confirm a PDF downloads. Open it: heading reads "Invoice" (NOT "Tax Invoice"), the INV number is shown, line items + total match, there is NO VAT row, and the bank details + "Thank you" footer are present.
    4. Click "Mark Paid" — confirm status becomes "Paid" and a paid timestamp shows. Confirm "Undo Paid" is available.
    5. Create another Draft, download its PDF — confirm the heading reads "Draft Invoice" with no number. Then delete it — confirm it disappears from the list.
    6. Confirm a Sent invoice with a past due date shows the red "Overdue" badge on both list and detail.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issue (edit lock, numbering, PDF labeling/VAT, badges, delete).</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0
- `npx vitest run src/app/api/admin/invoices` still green (no route regressions)
- Human checkpoint approves the full lifecycle: edit Draft → Sent (number assigned) → Paid, PDF labeling (no VAT), Draft delete, overdue badge
</verification>

<success_criteria>
- Detail page: edit for Draft, read-only for Sent/Paid; status buttons drive the full lifecycle
- Download PDF works with SARS-compliant labeling; Draft delete works
- Computed Overdue badge shown; auth-guarded; Tailwind v4 + btn classes + no per-section bg
</success_criteria>

<output>
After completion, create `.planning/phases/04-invoicing/04-05-SUMMARY.md` documenting: whether InvoiceForm was extended for edit (props added), the final button-visibility matrix per status, and any checkpoint feedback applied. Also note the STATE-tracked follow-up: the owner must run `npm run db:migrate` on the live DB before invoicing is usable, and record any @react-pdf/renderer mitigation carried over from 04-01.
</output>
