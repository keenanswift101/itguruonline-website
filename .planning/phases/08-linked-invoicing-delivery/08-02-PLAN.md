---
phase: 08-linked-invoicing-delivery
plan: 02
type: execute
wave: 1
depends_on: [08-01]
files_modified:
  - src/app/api/admin/invoices/route.ts
  - src/app/api/admin/invoices/route.test.ts
  - src/app/api/admin/invoices/[id]/route.ts
  - src/app/api/admin/invoices/[id]/route.test.ts
autonomous: true
requirements: [INVOICE-09, INVOICE-10]

must_haves:
  truths:
    - "POST /api/admin/invoices persists clientId when provided, storing the invoices.client_id link (INVOICE-09 SC2)"
    - "PUT /api/admin/invoices/[id] persists an edited clientId on a draft invoice"
    - "A POST/PUT with clientId=null (or omitted) creates/updates a valid free-text one-off invoice with client_id NULL (INVOICE-10 SC3)"
    - "A POST/PUT referencing a non-existent clientId returns 422 (not a raw 500 FK violation)"
    - "npx vitest run src/app/api/admin/invoices passes; DB tests gate on NETLIFY_DB_URL"
  artifacts:
    - path: src/app/api/admin/invoices/route.ts
      provides: "POST threads clientId into the withTxDb insert + validates the client exists"
      contains: "clientId: data.clientId"
    - path: src/app/api/admin/invoices/[id]/route.ts
      provides: "PUT threads clientId into the draft-only update"
      contains: "clientId: data.clientId"
  key_links:
    - from: src/app/api/admin/invoices/route.ts
      to: invoices.clientId
      via: "insert .values({ clientId: data.clientId ?? null, ... })"
      pattern: "clientId: data.clientId"
    - from: src/app/api/admin/invoices/route.ts
      to: clients
      via: "SELECT existence check before insert when clientId is set"
      pattern: "clients"
---

<objective>
Thread the optional `clientId` from `invoiceInput` (added in 08-01) through the invoice create (POST) and edit (PUT) routes so a picked client is persisted as `invoices.client_id`, while a null/omitted `clientId` keeps the existing free-text one-off path fully valid. Add a lightweight "client exists" check so a bad `clientId` returns a clean 422 instead of a raw FK-violation 500.

Purpose: This is the server contract behind INVOICE-09 (link stored) and INVOICE-10 (one-off still works). The 08-03 picker UI sends `clientId` in the request body; these routes are what make the link real. Backend-first so the UI in 08-03 has a route that actually honours the field.

Output: POST + PUT routes threading clientId (with existence validation), plus extended route tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-linked-invoicing-delivery/08-RESEARCH.md
@CLAUDE.md
@src/app/api/admin/invoices/route.ts
@src/app/api/admin/invoices/[id]/route.ts
@src/app/api/admin/invoices/route.test.ts
@src/app/api/admin/invoices/[id]/route.test.ts
@src/lib/invoices.ts
</context>

<interfaces>
<!-- From 08-01 (already on disk when this plan runs): -->
- invoiceInput now includes: `clientId: z.number().int().positive().nullable().optional()`
- invoices.clientId column exists (nullable FK to clients.id, ON DELETE SET NULL)

Existing POST (src/app/api/admin/invoices/route.ts) uses withTxDb → tx.insert(invoices).values({...}). The neon-http driver cannot run db.transaction() directly — MUST stay inside withTxDb (do NOT call db.transaction() on the http driver).

Existing PUT (src/app/api/admin/invoices/[id]/route.ts) draft-only lock: tx.update(invoices).set({...}).where(and(eq(id), eq(status,"draft"))). Only draft invoices are editable (409 otherwise).

`clients` table is imported from "@/lib/db/schema"; `db` from "@/lib/db/index"; `eq` from "drizzle-orm".
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: POST route — thread clientId + client-existence check</name>
  <files>src/app/api/admin/invoices/route.ts, src/app/api/admin/invoices/route.test.ts</files>
  <read_first>src/app/api/admin/invoices/route.ts (full — withTxDb insert), src/app/api/admin/invoices/route.test.ts (validBody + mocked-session pattern)</read_first>
  <action>
    In `src/app/api/admin/invoices/route.ts`:
    1. Add imports: `import { db } from "@/lib/db/index";`, `import { clients } from "@/lib/db/schema";` (keep the existing `invoices, invoiceLineItems` import), and `import { eq } from "drizzle-orm";`.
    2. After `const data = parsed.data;` and BEFORE the withTxDb write, add the existence guard (only when a clientId was supplied):
       ```typescript
       if (data.clientId != null) {
         const [client] = await db
           .select({ id: clients.id })
           .from(clients)
           .where(eq(clients.id, data.clientId));
         if (!client) {
           return NextResponse.json(
             { error: "Validation failed.", fields: { clientId: ["Client not found."] } },
             { status: 422 }
           );
         }
       }
       ```
    3. In the `tx.insert(invoices).values({ ... })` call, add as the first value:
       `clientId: data.clientId ?? null,`
    Leave the line-items insert and the `{ ok: true, id }` 201 response unchanged.

    In `src/app/api/admin/invoices/route.test.ts`, add non-DB + DB stubs:
    - Non-DB guard: `it.todo("422 when clientId references a non-existent client")` — or implement it against the mocked-db pattern if the file mocks db; otherwise leave as it.todo.
    - DB (describeIfDb, gate `process.env.NETLIFY_DB_URL`): `it.todo("persists client_id when a valid clientId is supplied")` and `it.todo("creates a valid one-off invoice with client_id NULL when clientId is omitted")`.
    Keep all existing tests intact.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/app/api/admin/invoices/route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'clientId: data.clientId ?? null' src/app/api/admin/invoices/route.ts` matches
    - `grep -n 'fields: { clientId: \["Client not found."\] }' src/app/api/admin/invoices/route.ts` matches
    - `grep -n 'clientId' src/app/api/admin/invoices/route.test.ts` matches
    - existing POST tests still pass; npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>POST persists clientId (or null), rejects an unknown clientId with 422, and existing behaviour is unchanged; tests extended.</done>
</task>

<task type="auto">
  <name>Task 2: PUT route — thread clientId on draft edit</name>
  <files>src/app/api/admin/invoices/[id]/route.ts, src/app/api/admin/invoices/[id]/route.test.ts</files>
  <read_first>src/app/api/admin/invoices/[id]/route.ts (full — draft-only update + existing db/eq imports)</read_first>
  <action>
    In `src/app/api/admin/invoices/[id]/route.ts`:
    1. `db`, `eq`, `and`, and `clients`? — `db` and `eq`/`and` are already imported; add `clients` to the existing `import { invoices, invoiceLineItems } from "@/lib/db/schema";` line.
    2. After `const data = parsed.data;` and BEFORE the withTxDb block, add the same existence guard as the POST route (only when clientId supplied):
       ```typescript
       if (data.clientId != null) {
         const [client] = await db
           .select({ id: clients.id })
           .from(clients)
           .where(eq(clients.id, data.clientId));
         if (!client) {
           return NextResponse.json(
             { error: "Validation failed.", fields: { clientId: ["Client not found."] } },
             { status: 422 }
           );
         }
       }
       ```
    3. In the `tx.update(invoices).set({ ... })` call, add: `clientId: data.clientId ?? null,`
    Keep the draft-only WHERE guard, the EditLockError throw/409 handling, and the line-items replace unchanged. Editing an auto-filled name/email must KEEP the link — that is honoured automatically because the UI sends the same clientId (locked decision: editing text does not unlink).

    In `src/app/api/admin/invoices/[id]/route.test.ts`, add:
    - Non-DB or mocked: keep existing 401/404/409 tests intact.
    - DB (describeIfDb): `it.todo("persists an edited clientId on a draft invoice")` and `it.todo("keeps client_id NULL for a free-text edit")`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/app/api/admin/invoices/[id]/route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'clientId: data.clientId ?? null' src/app/api/admin/invoices/[id]/route.ts` matches
    - `grep -n 'fields: { clientId: \["Client not found."\] }' src/app/api/admin/invoices/[id]/route.ts` matches
    - `grep -n 'clientId' src/app/api/admin/invoices/[id]/route.test.ts` matches
    - existing PUT/DELETE tests still pass; npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>PUT persists an edited clientId on drafts (or null for free-text), rejects unknown clientId with 422; draft-lock + tests intact.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — exits 0
2. `npx vitest run src/app/api/admin/invoices` — passes
3. `grep -c 'clientId: data.clientId ?? null' src/app/api/admin/invoices/route.ts src/app/api/admin/invoices/[id]/route.ts` shows the thread in both
</verification>

<success_criteria>
- POST + PUT persist clientId (or NULL for one-off)
- Unknown clientId → 422, not a raw 500
- Free-text one-off path unchanged (INVOICE-10)
- Route tests extended, existing tests green
</success_criteria>

<output>
After completion, create `.planning/phases/08-linked-invoicing-delivery/08-02-SUMMARY.md`
</output>
